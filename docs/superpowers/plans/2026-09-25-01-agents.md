# Cluster 01 "agents" Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make every change the spec asks of `.agents/**` and `.claude/agents/*.md`: drop the verification-diary rule, give `agent-police` an external measure, confine every role to the machine population, and bring `conformance.md` in line with §4.2 and §4.1.

**Architecture:** Text edits only. Each task replaces quoted passages with quoted text, then proves the result with `grep`. The machine population is defined once, in `github-needs`; `pipeline-law` cites it and every other role names it. Nothing here runs a client or calls a model.

**Tech Stack:** Markdown and YAML files; `grep`; `tools/check-conformance.py` in the scratchpad venv; `git`.

## Global Constraints

Copied from the spec (`docs/superpowers/specs/2026-09-24-marketplace-quality-design.md`) and the conventions (`docs/superpowers/plans/2026-09-25-00-conventions.md`):

- "The repository does not record the maintainer's own verification." (spec §2)
- "Pipelines are private: public docs never mention the roles; agents never touch issues filed by people." (spec §2)
- "The nine-role agent system stays in composition." (spec §2)
- "Every copy of a file is generated on CI from one source; nothing is kept by hand twice." (spec §2)
- "No paid tests: nothing in CI or in the packages calls a model." (spec §2)
- `slop.md` line "**The instructions.** `.agents/**`. Read, never judged." stays as it is (spec §3.1).
- The machine population is "label `police-report` and a body line `police-fingerprint: <role> <value>`. Open issues with `repo-audit-routine:`, `slop-police-fingerprint:` or `agent-police-fingerprint:` count until closed. `pipeline-law` cites the definition; every other skill names 'the machine population' and does not restate it." (conventions, `.agents/`)
- Prose: every new sentence follows `plugins/prose-discipline/rules/prose-discipline.md`: at most 25 words, active voice, no filler, no maintainer diary, English. A sentence about a client cites its documentation or source by link, with the kind named; a source link is a commit permalink. (conventions, Prose)
- Commits: conventional commits, one per task, body sentences of at most 25 words, last line `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`. (conventions, Commits)
- Conformance check: `/private/tmp/claude-501/-Users-akurganow-Projects-ai-plugins/aa53b02e-f388-4b09-bc13-baf4eb5c52bb/scratchpad/venv/bin/python tools/check-conformance.py`. Exit 0 is the only pass. Below it is written `$PY tools/check-conformance.py`, with `PY=/private/tmp/claude-501/-Users-akurganow-Projects-ai-plugins/aa53b02e-f388-4b09-bc13-baf4eb5c52bb/scratchpad/venv/bin/python`.

## How to read this plan

- Every quoted "Current text" was read at `344c8fc`. Line numbers are there to find the passage; the quoted text is what the Edit tool must match. Re-read the file before each edit.
- Tasks that edit the same file quote disjoint passages, so their order inside a file does not matter. Run the tasks in the order given anyway.
- Tasks 8–12 name paths that other plans create: `tools/regenerate.sh` (plan 02), `plugins/howp/skills/forecast/` (plan 04), the README's `Install` section (plan 06), `CONTRIBUTING.md`, `SUPPORT.md` (plan 06), `.github/workflows/release-please.yml` and the changelogs (plan 07). All plans land on one branch before merge, so the names are true at merge.
- The check reads no file under `.agents/`. It runs after every task only to prove the tree still conforms.
- The review of this plan decided the open questions and added four changes: A (Task 8, step 5), B (Task 11), C (Task 12) and D (Task 2, step 7). **Decisions taken at review**, at the end, records them.

## File structure

| File | Tasks | What changes |
| :-- | :-- | :-- |
| `.agents/rules/claims.md` | 1, 12 | diary bullet deleted; release-please's files |
| `.agents/rules/slop.md` | 1, 9, 10, 12 | diary protection gone; the fence and the opening name the regeneration script; howp path; release-please's files |
| `.agents/rules/conformance.md` | 8, 9, 10, 12 | vendor manifest bullet and the Codex documentation clause; "Text only"; howp path; "Versions" |
| `.agents/prompts/project.md` | 1, 8 | diary clause; vendor manifest as a copy |
| `.agents/policies/repository.yaml` | 11 | `description` and `paths.allow` |
| `.agents/manifest.yaml` | 2 | `shared.github-needs` names the Agent Police and the definition |
| `AGENTS.md` | 2, 12 | five roles read `github-needs`; the "Versions" bullet names both release jobs |
| `.agents/skills/github-needs/SKILL.md` | 2 | the one definition of the machine population |
| `.agents/skills/agent-police/SKILL.md` | 3, 4, 8 | external measure; fingerprint line; read 5's reason |
| `.claude/agents/agent-police.md` | 3 | description |
| `.agents/skills/repo-police/SKILL.md` | 1, 4, 10 | diary clause; fingerprint line; README section name |
| `.agents/skills/slop-police/SKILL.md` | 1, 4, 9, 12 | diary protections; fingerprint line; two programs; release-written files uncounted |
| `.agents/skills/issue-court/SKILL.md` | 1, 5, 10 | diary clause; population, routing, third authority; README section name |
| `.claude/agents/issue-court.md` | 5 | description |
| `.agents/skills/tracker-clerk/SKILL.md` | 6, 10 | population; README section name |
| `.agents/skills/pipeline-law/SKILL.md` | 7, 9, 12 | cites the definition; parts; two programs; verification; `CHANGELOG.md` forbidden |
| `.agents/skills/pipeline-clerk/SKILL.md` | 7 | intake from the population; a part is filed into it |
| `.agents/skills/spec-writer/SKILL.md` | 1, 9 | diary clause; verification |
| `.agents/skills/implementer/SKILL.md` | 1, 9, 12 | diary clause; regeneration in the exit criteria; `CHANGELOG.md` in the staged-path check |

Checked and left unchanged: `.agents/prompts/base.md`, `.agents/modes/maintainer.md`, `.agents/rules/unattended.md`, `.agents/skills/spec-reviewer/SKILL.md`, and the other seven files under `.claude/agents/`. None carries text the spec changes.

---

### Task 1: Remove the verification-diary rule (spec §3.1)

**Files:**
- Modify: `.agents/rules/claims.md:22-27`
- Modify: `.agents/rules/slop.md:34-37`, `:83-85`, `:97-98`
- Modify: `.agents/skills/slop-police/SKILL.md:313`, `:337-347`, `:418`
- Modify: `.agents/skills/spec-writer/SKILL.md:203-206`
- Modify: `.agents/skills/implementer/SKILL.md:483-485`
- Modify: `.agents/prompts/project.md:30-32`
- Modify: `.agents/skills/repo-police/SKILL.md:20-23`
- Modify: `.agents/skills/issue-court/SKILL.md:28-31`

**Interfaces:**
- Consumes: nothing.
- Produces: the protected-list entry "**A cited source and its date.**" in `slop.md`, and its short names "a cited source and its date" and `cited source` in `slop-police`. No later task renames them.

- [ ] **Step 1: Run the test and see it fail**

```bash
grep -rnE 'was not verified|"not verified"|unverified (stated|means)|claims discipline.s sentences|claims sentence|What has been|install section|`claims.md` protects' .agents
```

Expected: 13 lines, in `claims.md`, `slop.md` (3), `project.md`, `implementer`, `slop-police` (4), `spec-writer`, `repo-police`, `issue-court`.

- [ ] **Step 2: `claims.md` — delete the bullet**

Current text (lines 22–27), delete entirely:

````text
- **Say what was not verified.** `README.md` opens the install section by
  saying nothing below has been installed from this repository as published.
  That sentence is load-bearing; it is not softened, moved to a footnote, or
  quietly upgraded because a package "should" work — and testing a fix in a
  working tree does not retire it, because what is published is what a reader
  will install.
````

Replacement: nothing. The bullet before it ("**Cite the source, in the text, and say which kind it is.**") is followed directly by "**Never invent a command.**".

- [ ] **Step 3: `slop.md` kind 1 — delete the "Not noise" pair**

Current text (lines 33–37):

````text
   complete. Measurement: the content words against the text and code
   beside them, nothing left over. Not noise: a sentence that says what was
   not verified. `claims.md` makes that sentence load-bearing — the one
   `README.md` opens its install section with carries a fact a reader acts
   on, and reads like a hedge only to an eye that has not read `claims.md`.
````

New text:

````text
   complete. Measurement: the content words against the text and code
   beside them, nothing left over.
````

- [ ] **Step 4: `slop.md` "What is protected" — keep only the source and the date**

Current text (lines 83–85):

````text
- **The claims discipline's sentences.** The statement of what was not
  verified, the per-fact source and its kind, a date and a tag beside a
  measurement. To an outside eye these read as hedging; each is a fact.
````

New text:

````text
- **A cited source and its date.** The per-fact source and its kind, a date
  and a tag beside a measurement. To an outside eye these read as hedging;
  each is a fact.
````

- [ ] **Step 5: `slop.md` "The fence" — name what `claims.md` still requires**

Current text (lines 97–98):

````text
nothing should — words have legitimate readings, and the sentence
`claims.md` protects would be the first casualty of a hedge filter.
````

New text:

````text
nothing should — words have legitimate readings, and the sourced, dated
sentences `claims.md` requires would be the first casualties of a hedge filter.
````

- [ ] **Step 6: `slop-police` — follow the protected list**

Current text (line 313):

````text
- the claims discipline's sentences
````

New text:

````text
- a cited source and its date
````

Current text (lines 337–347):

````text
- **`README.md`.** Paragraph by paragraph: each against the one before
  it (`noise`), against the check and the tree (`lying`), and for
  passages that describe an update rather than the thing (`residue`).
  The install section's opening sentence and every "not verified" line
  are `claims.md`'s and protected.
- **The skills.** Every `plugins/*/skills/*/SKILL.md` and its
  `references/`, and every plugin's own README — except
  `references/commands.md` under `howp`, which the release writes and
  nobody judges here. A step the procedures describe against what the
  text beside it says the binary or the rule does; a "What has been
  verified" section is a claims record and protected.
````

New text:

````text
- **`README.md`.** Paragraph by paragraph: each against the one before
  it (`noise`), against the check and the tree (`lying`), and for
  passages that describe an update rather than the thing (`residue`).
- **The skills.** Every `plugins/*/skills/*/SKILL.md` and its
  `references/`, and every plugin's own README — except
  `references/commands.md` under `howp`, which the release writes and
  nobody judges here. A step the procedures describe against what the
  text beside it says the binary or the rule does.
````

Current text (line 418):

````text
    protected: none | recorded reason | owner's quotation | release-written | claims sentence | house style
````

New text:

````text
    protected: none | recorded reason | owner's quotation | release-written | cited source | house style
````

- [ ] **Step 7: `spec-writer` — delete the clause**

Current text (lines 203–206):

````text
Every new sentence about a client, an install command or a released artifact
is held to `claims.md`. It names its source. It says which kind of source.
It says what was not verified. A specification proposing an unsourced claim
is one the Reviewer rejects.
````

New text:

````text
Every new sentence about a client, an install command or a released artifact
is held to `claims.md`. It names its source. It says which kind of source.
A specification proposing an unsourced claim is one the Reviewer rejects.
````

- [ ] **Step 8: `implementer` — drop the claims justification of the handover section**

Current text (lines 483–485):

````text
   The fourth section is the one thing in the handover that cannot be
   skipped. It turns "not verified" into the owner's checklist, and
   `.agents/rules/claims.md` is why.
````

New text:

````text
   The fourth section is the one thing in the handover that cannot be
   skipped. It is the owner's checklist of what no command here can prove.
````

- [ ] **Step 9: `project.md` — delete the clause**

Current text (lines 30–32):

````text
- **Every compatibility claim is sourced.** Install instructions, supported
  surfaces, loader behaviour — each names the documentation or the source
  file it was read from. What was not verified says so.
````

New text:

````text
- **Every compatibility claim is sourced.** Install instructions, supported
  surfaces, loader behaviour — each names the documentation or the source
  file it was read from.
````

- [ ] **Step 10: `repo-police` and `issue-court` — drop the summaries of the deleted bullet**

`repo-police`, current text (lines 20–23):

````text
2. `.agents/rules/claims.md` — the claims discipline this repository lives
   by: documentation first, every claim cites its source and kind,
   unverified means stated as unverified. Your findings are held to it, and
   violations of it in the repo are among your best findings.
````

New text:

````text
2. `.agents/rules/claims.md` — the claims discipline this repository lives
   by: documentation first, every claim cites its source and kind. Your
   findings are held to it, and violations of it in the repo are among your
   best findings.
````

`issue-court`, current text (lines 28–31):

````text
2. `.agents/rules/claims.md` — the claims discipline: documentation first,
   sources cited and dated, unverified stated as unverified. An issue
   alleging a violation of it is squarely a case; a verdict that itself
   violated it would be worthless.
````

New text:

````text
2. `.agents/rules/claims.md` — the claims discipline: documentation first,
   sources cited and dated. An issue alleging a violation of it is squarely
   a case; a verdict that itself violated it would be worthless.
````

- [ ] **Step 11: Run the test and see it pass**

```bash
grep -rnE 'was not verified|"not verified"|unverified (stated|means)|claims discipline.s sentences|claims sentence|What has been|install section|`claims.md` protects' .agents
grep -n 'A cited source and its date' .agents/rules/slop.md
grep -n 'Read, never judged' .agents/rules/slop.md
grep -c 'Never invent a command' .agents/rules/claims.md
```

Expected: the first prints nothing. The second prints one line. The third prints the `.agents/**` bullet, unchanged. The fourth prints `1`.

- [ ] **Step 12: Run the conformance check**

Run: `$PY tools/check-conformance.py`
Expected: `conformance OK: 6 plugin(s) checked (…)`, exit 0.

- [ ] **Step 13: Commit**

```bash
git add .agents/rules/claims.md .agents/rules/slop.md .agents/prompts/project.md \
  .agents/skills/slop-police/SKILL.md .agents/skills/spec-writer/SKILL.md \
  .agents/skills/implementer/SKILL.md .agents/skills/repo-police/SKILL.md \
  .agents/skills/issue-court/SKILL.md
git commit -F - <<'EOF'
chore: drop the rule that asks for a verification diary

claims.md no longer requires a sentence saying what was not verified.
slop.md, the roles and project.md stop protecting or demanding that sentence.
A cited source and its date stay protected.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
EOF
```

---

### Task 2: Define the machine population once, in `github-needs` (spec §3.3)

**Files:**
- Modify: `.agents/skills/github-needs/SKILL.md` (front matter, lines 8–9, a new section, lines 26–32)
- Modify: `.agents/manifest.yaml:91-93`
- Modify: `AGENTS.md:37-39` (review addition D; `AGENTS.md` changes only here and in task 12's "Versions" bullet; `CLAUDE.md` is a symlink to it)

**Interfaces:**
- Consumes: nothing.
- Produces, used by tasks 4–7 by these exact names:
  - the heading `## The machine population` in `.agents/skills/github-needs/SKILL.md`;
  - the fingerprint line `<!-- police-fingerprint: <role> <value> -->`, where `<role>` is a skill name: `repo-police`, `slop-police`, `agent-police`, `pipeline-clerk`;
  - the phrase "the machine population" and "a role's own issues";
  - the filer rule: every filer applies `police-report`, and a filer that cannot confirm it files nothing.

- [ ] **Step 1: Run the test and see it fail**

```bash
grep -n 'The machine population\|police-fingerprint:' .agents/skills/github-needs/SKILL.md
grep -n 'agent-police' .agents/manifest.yaml
```

Expected: the first prints nothing. The second prints only line 31 (`- agent-police` under `enabled.skills`).

```bash
grep -n 'four analysis roles share' AGENTS.md
```

Expected: one line, line 37.

- [ ] **Step 2: Replace the description**

Current text (line 3):

````text
description: "What an unattended analysis run of this repository needs from GitHub, stated as needs and never as routes: the probe, the listings, the reads of one issue, the label write that must send the whole set back, and the close. Read it before acting as the Issue Court, the Tracker Clerk, the Repo Police or the Slop Police."
````

New text:

````text
description: "What an unattended analysis run of this repository needs from GitHub, stated as needs and never as routes: the machine population, the probe, the listings, the reads of one issue, the label write that must send the whole set back, and the close. Read it before acting as the Issue Court, the Tracker Clerk, the Repo Police, the Slop Police or the Agent Police."
````

- [ ] **Step 3: Replace the readers paragraph**

Current text (lines 8–9):

````text
This file is one file and the four analysis agents all read it: the Issue
Court, the Tracker Clerk, the Repo Police and the Slop Police.
````

New text:

````text
This file is one file and the four analysis agents all read it: the Issue
Court, the Tracker Clerk, the Repo Police and the Slop Police. The Agent
Police reads it too. The four pipeline roles read its first section, which
`pipeline-law` cites.
````

- [ ] **Step 4: Insert the definition and a heading for the needs**

Current text (lines 14–17):

````text
Each environment has its own, so the route stays with whatever fired
you.

- **GitHub.** Reach it through whatever route this environment gives
````

New text:

````text
Each environment has its own, so the route stays with whatever fired
you.

## The machine population

This is the one definition. Every other skill names the machine population
and points here; none restates it.

An issue is in the machine population when both hold:

- it carries the label `police-report`;
- its body carries the fingerprint line, an HTML comment of the form
  `<!-- police-fingerprint: <role> <value> -->`. `<role>` is the skill name
  of the role that filed it. `<value>` is that role's identity for the
  finding, in the form its skill gives.

An open issue carrying `police-report` and one of three older markers is in
the population too, until it is closed. Each marker belongs to one role:
`repo-audit-routine:` to `repo-police`, `slop-police-fingerprint:` to
`slop-police`, and `agent-police-fingerprint:` to `agent-police`. Every
filer writes only the fingerprint line.

A role's own issues are the issues in the population whose fingerprint line
or older marker names that role.

Every other issue does not exist for any role. No role lists it, reads it,
comments on it, labels it or closes it.

Every filer applies `police-report` when it files. A filer that cannot
confirm the label on the repository's label list files nothing, and says so
in its report.

## The needs

- **GitHub.** Reach it through whatever route this environment gives
````

- [ ] **Step 5: Make the label mandatory in every issue listing, and drop the tracker-wide search**

Current text (lines 26–32):

````text
  - issues by label and state, with full bodies, paginated to the end.
    A route may return pull requests in an issue listing: filter them
    out;
  - pull requests, open or all, with number, title, head, labels and
    body, and the paths each one touches;
  - a fingerprint anywhere in the tracker: a search may be inexact, so
    confirm the marker in each hit's body;
````

New text:

````text
  - issues by label and state, with full bodies, paginated to the end.
    The label is mandatory: every issue listing names `police-report`,
    alone or beside another label, and none is ever made without it. A
    route may return pull requests in an issue listing: filter them out;
  - pull requests, open or all, with number, title, head, labels and
    body, and the paths each one touches;
````

- [ ] **Step 6: `manifest.yaml` — name the new reader and the definition**

Current text (lines 91–93):

````text
        github-needs: >-
          Read by issue-court, tracker-clerk, repo-police and slop-police.
          The same, for what a run needs from GitHub.
````

New text:

````text
        github-needs: >-
          Read by issue-court, tracker-clerk, repo-police, slop-police and
          agent-police. The same, for what a run needs from GitHub. It
          holds the one definition of the machine population, which
          pipeline-law cites for the four pipeline roles.
````

- [ ] **Step 7: `AGENTS.md` — the count of `github-needs` readers follows the manifest (review addition D)**

`AGENTS.md` is a real file; `CLAUDE.md` is a symlink to it, so edit `AGENTS.md` only. The Agent Police's binding already preloads `github-needs`, and after step 6 the manifest names it as a reader.

Current text (lines 35–39):

````text
Nine roles, as skills under `.agents/skills/`, each with one thin Claude
binding under `.claude/agents/` that names its skills and carries no role text.
Four pipeline roles share `pipeline-law`; four analysis roles share
`github-needs`; the ninth patrols the other eight. `.agents/manifest.yaml`
declares the set and records why each arrangement is what it is.
````

New text:

````text
Nine roles, as skills under `.agents/skills/`, each with one thin Claude
binding under `.claude/agents/` that names its skills and carries no role text.
Four pipeline roles share `pipeline-law`. Five roles read `github-needs`: the
four analysis roles, and the ninth, which patrols the other eight.
`.agents/manifest.yaml` declares the set and records why each arrangement is
what it is.
````

- [ ] **Step 8: Run the test and see it pass**

```bash
grep -n '^## The machine population$\|^## The needs$\|police-fingerprint: <role> <value>\|The label is mandatory' .agents/skills/github-needs/SKILL.md
grep -n 'a fingerprint anywhere in the tracker' .agents/skills/github-needs/SKILL.md
grep -n 'agent-police. The same' .agents/manifest.yaml
grep -n 'four analysis roles share\|Five roles read `github-needs`' AGENTS.md
test -L CLAUDE.md && echo symlink-kept
$PY -c "import yaml; yaml.safe_load(open('.agents/manifest.yaml')); t=open('.agents/skills/github-needs/SKILL.md').read(); d=yaml.safe_load(t.split('---')[1]); assert d['name']=='github-needs'; print('yaml OK')"
```

Expected: the first prints four lines. The second prints nothing. The third prints one line. The fourth prints one line, the new sentence. The fifth prints `symlink-kept`. The sixth prints `yaml OK`.

- [ ] **Step 9: Run the conformance check**

Run: `$PY tools/check-conformance.py`
Expected: exit 0.

- [ ] **Step 10: Commit**

```bash
git add .agents/skills/github-needs/SKILL.md .agents/manifest.yaml AGENTS.md
git commit -F - <<'EOF'
chore: define the machine population once, in github-needs

An issue exists for a role only with police-report and a police-fingerprint line.
Open issues with the three older markers count until closed.
Every issue listing names police-report.
AGENTS.md now counts five readers of github-needs, as the manifest does.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
EOF
```

---

### Task 3: Give `agent-police` an external measure (spec §3.2)

**Files:**
- Modify: `.agents/skills/agent-police/SKILL.md` (front matter, lines 55–68, a new section before line 83, lines 234–251, lines 253–270)
- Modify: `.claude/agents/agent-police.md:3`

**Interfaces:**
- Consumes: nothing.
- Produces, used by task 5 by these exact names: the heading `## External sources` and `## The external pass` in `.agents/skills/agent-police/SKILL.md`; the finding kind `external-disagreement`; the third authority, "the published source it quotes".

- [ ] **Step 1: Run the test and see it fail**

```bash
grep -n 'external-disagreement\|^## External sources\|the right machine' .agents/skills/agent-police/SKILL.md .claude/agents/agent-police.md
```

Expected: nothing.

- [ ] **Step 2: Replace the skill description**

Current text (line 3 of `.agents/skills/agent-police/SKILL.md`):

````text
description: "Audit this repository's own agent system for internal disagreement: a role that contradicts the shared law, a vocabulary kept in two places, a binding that names something absent, a document nothing reads. File the few clusters a maintainer would clear at once. Use for the patrol of the agents themselves."
````

New text:

````text
description: "Audit this repository's own agent system for internal disagreement and for disagreement with the published sources it rests on: a role that contradicts the shared law, a rule that contradicts a specification or a client's documentation, a vocabulary kept in two places, a binding that names something absent, a document nothing reads. File the few clusters a maintainer would clear at once. Use for the patrol of the agents themselves."
````

- [ ] **Step 3: Replace the binding description**

Current text (line 3 of `.claude/agents/agent-police.md`):

````text
description: "Audit this repository's own agent system for internal disagreement: a role that contradicts the shared law, a vocabulary kept in two places, a binding that names something absent, a document nothing reads. Use when an unattended run must patrol the agents themselves and file what a maintainer would clear at once."
````

New text:

````text
description: "Audit this repository's own agent system for internal disagreement and for disagreement with the published sources it rests on: a role that contradicts the shared law, a rule that contradicts a specification or a client's documentation, a vocabulary kept in two places, a binding that names something absent, a document nothing reads. Use when an unattended run must patrol the agents themselves and file what a maintainer would clear at once."
````

- [ ] **Step 4: Add the second question and widen the boundary table**

Current text (lines 53–68):

````text
and the prompts. Count them from the tree each run and never from this
sentence — a count written down here is a count that goes stale the next time
a role is added. One question over all of it:

> Do these documents still describe one machine, or have they begun to
> describe two?

That is a different question from every other role's, and the boundary is
worth stating because three of them run over the same tree:

| Role | Asks |
| :-- | :-- |
| the repository auditor | is what this repository publishes to a stranger true, and does it install |
| the Slop Police | does this sentence carry a fact, anywhere but `.agents/**` |
| the Issue Court | is this filed finding real |
| **you** | do the agent system's own documents agree with each other |
````

New text:

````text
and the prompts. Count them from the tree each run and never from this
sentence — a count written down here is a count that goes stale the next time
a role is added. Two questions over all of it:

> Do these documents still describe one machine, or have they begun to
> describe two?

> Is it the right machine: does what these documents require agree with the
> published sources under **External sources**?

The mechanical and reading passes answer the first. The second needs a
measure from outside the tree, because rules that agree with each other can
all be wrong the same way. The external pass answers it.

Those are different questions from every other role's, and the boundary is
worth stating because three of them run over the same tree:

| Role | Asks |
| :-- | :-- |
| the repository auditor | is what this repository publishes to a stranger true, and does it install |
| the Slop Police | does this sentence carry a fact, anywhere but `.agents/**` |
| the Issue Court | is this filed finding real |
| **you** | do the agent system's own documents agree with each other, and with the published sources |
````

- [ ] **Step 5: Insert the source list and the external pass before the mechanical pass**

Current text (lines 83–85):

````text
## The mechanical pass

Nine reads, in this order. **Each says beside itself what it enforces** — a
````

New text:

````text
## External sources

Read these before any file under `.agents/`. Each site is the source of
record. Where a repository publishes the same text, read that copy when the
site is blocked. Keep the copy under `$RUN` and cite it by file and commit. A
source neither route serves is reported as blocked, with the reply the
environment gave. The comparisons that lean on it are then reported as not
run.

- **Agent Plugins specification.** https://agent-plugins.org/specification;
  repository https://github.com/agentplugins/agent-plugins-spec, files
  `spec/1.0.0.md` and `schemas/1.0.0/`. Check every rule about package
  shape, fixed locations, manifest fields and what a client must do.
- **Agent Skills specification.** https://agentskills.io/specification;
  repository https://github.com/agentskills/agentskills, file
  `docs/specification.mdx`. Check every rule about a skill's directory,
  `name`, `description` and front matter.
- **Claude Code plugin documentation.**
  https://code.claude.com/docs/en/plugins, and `plugins-reference`,
  `plugin-marketplaces`, `skills`, `hooks` and `sub-agents` under the same
  path; `.md` appended returns the Markdown. Check every rule about the
  vendor manifest, the catalogue, hooks and agent bindings.
- **Codex plugin documentation.** https://learn.chatgpt.com/docs/plugins,
  https://learn.chatgpt.com/docs/build-skills,
  https://learn.chatgpt.com/docs/hooks.md and
  https://developers.openai.com/plugins/build/plugins.md. The repository
  https://github.com/openai/codex holds no copy: its `docs/` only points at
  these pages. Check every rule about how Codex reads a package.
- **Hermes plugin documentation.**
  https://hermes-agent.nousresearch.com/docs/developer-guide/plugins;
  repository https://github.com/NousResearch/hermes-agent, files
  `website/docs/developer-guide/plugins/index.md`,
  `website/docs/user-guide/features/plugins.md`,
  `website/docs/user-guide/features/skills.md` and
  `website/docs/user-guide/cli.md`. Check every rule about the portable
  subset and skill names.
- **Oh-My-Pi plugin documentation.** https://omp.sh/docs/marketplace;
  repository https://github.com/can1357/oh-my-pi, files
  `docs/marketplace.md` and `docs/skills.md`. Check every rule about the
  catalogue path and skill-name collisions.
- **standard-readme.** Repository
  https://github.com/RichardLitt/standard-readme, file `spec.md`. Check
  every rule about a README's sections and their order.
- **Keep a Changelog.** https://keepachangelog.com/en/1.1.0/; repository
  https://github.com/olivierlacan/keep-a-changelog, file
  `source/en/1.1.0/index.html.haml`. Check every rule about a changelog.
- **Semantic Versioning.** https://semver.org/; repository
  https://github.com/semver/semver, file `semver.md`. Check every rule
  about versions and tags.
- **The ecosystems' listing rules.** The Claude Code community marketplace:
  https://code.claude.com/docs/en/plugins.md, section "Submit your plugin to
  the community marketplace". The Hermes catalogue: `plugin-catalog/README.md`
  in https://github.com/NousResearch/hermes-agent. The awesome lists: the
  `CONTRIBUTING.md` of https://github.com/hesreallyhim/awesome-claude-code,
  https://github.com/travisvn/awesome-claude-skills and
  https://github.com/ComposioHQ/awesome-claude-skills. Check every rule about
  what a listed package must carry.

## The external pass

Read every source under **External sources** before you open any file under
`.agents/`. What the sources say is then not shaped by what the rules say.

Then read the rule files and the roles against them. A source speaks only to
what it governs. That is how a package, a manifest, a skill, a README, a
changelog or a version must look. It is also how a client reads them. The
pipeline's labels and markers have no outside measure, and this pass does
not judge them.

A rule or a role that requires what a source forbids, or forbids what a
source requires, is an `external-disagreement`. Its exhibit is two quotes:
the rule's, with its path and line, and the source's, linked in place with
its kind named. A repository copy is cited by file and commit, never by a
branch. The finding is filed like every other, under the same cap.

A source that is silent is not a disagreement. A rule that goes further than
every source, and contradicts none, is no finding.

## The mechanical pass

Nine reads, in this order. **Each says beside itself what it enforces** — a
````

- [ ] **Step 6: Add the third authority**

Current text (lines 234–237):

````text
**Which rulebook judges you.** The Court judges an auditor's finding by
`claims.md` and a Slop Police finding by `slop.md`. Yours has **two
authorities, one per pass, and the issue names which**, because the two are
judged on different questions.
````

New text:

````text
**Which rulebook judges you.** The Court judges an auditor's finding by
`claims.md` and a Slop Police finding by `slop.md`. Yours has **three
authorities, one per pass, and the issue names which**, because the three
are judged on different questions.
````

Current text (lines 249–251):

````text
the issue claims it says. A read whose output you cannot show is not a
mechanical finding at all; it is the report line the paragraph above calls
for.
````

New text:

````text
the issue claims it says. A read whose output you cannot show is not a
mechanical finding at all; it is the report line the paragraph above calls
for.

An **external** finding is judged by the published source it quotes. Name
the source as **External sources** lists it, with the link and the commit
you read. The Court's question is whether the source says what the issue
quotes, and whether the rule contradicts it.
````

- [ ] **Step 7: Report the sources**

Current text (lines 255–270):

````text
The five-part shape from the tracker discipline, with two additions of your
own:

- **The nine reads**, each as run with what it printed, or as not run with
  why. Never omitted, never summarised as "all clean" without the outputs.
- **One line saying what you could not check**: the callers are not in the
  tree, so nothing here says whether anything fires a role, still
  points at one, or has ever fired.

When nothing survived and all nine ran, the Filed line reads `Filed nothing.
SYSTEM CONSISTENT — no findings at <sha>.` with the commit you analysed in
it. Where any read did not run it reads `Filed nothing. PATROL INCOMPLETE at
<sha> — <n> reads not run.` instead, and never the first: a patrol that could
not run a read audited less than it claims, and
`.agents/rules/unattended.md`'s rule is that a check not run is reported as
not run rather than as passing.
````

New text:

````text
The five-part shape from the tracker discipline, with three additions of your
own:

- **The nine reads**, each as run with what it printed, or as not run with
  why. Never omitted, never summarised as "all clean" without the outputs.
- **The external sources**, each as read, with file and commit for a
  repository copy, or as blocked, with the environment's reply.
- **One line saying what you could not check**: the callers are not in the
  tree, so nothing here says whether anything fires a role, still
  points at one, or has ever fired.

When nothing survived, all nine ran and every source was read, the Filed line
reads `Filed nothing. SYSTEM CONSISTENT — no findings at <sha>.` with the
commit you analysed in it. Otherwise it reads `Filed nothing. PATROL
INCOMPLETE at <sha> — <n> reads or sources not run.` and never the first. A
patrol that could not run a read audited less than it claims.
`.agents/rules/unattended.md` says a check not run is reported as not run,
never as passing.
````

- [ ] **Step 8: Run the test and see it pass**

```bash
grep -n 'external-disagreement\|^## External sources$\|^## The external pass$\|the right machine\|three$' .agents/skills/agent-police/SKILL.md
grep -c 'published sources it rests on' .agents/skills/agent-police/SKILL.md .claude/agents/agent-police.md
$PY -c "
import yaml
for f in ['.agents/skills/agent-police/SKILL.md','.claude/agents/agent-police.md']:
    d=yaml.safe_load(open(f).read().split('---')[1]); assert d['name']=='agent-police', f
print('front matter OK')"
```

Expected: the first prints at least five lines, including both headings. The second prints `1` for each file. The third prints `front matter OK`.

- [ ] **Step 9: Run the conformance check**

Run: `$PY tools/check-conformance.py`
Expected: exit 0.

- [ ] **Step 10: Commit**

```bash
git add .agents/skills/agent-police/SKILL.md .claude/agents/agent-police.md
git commit -F - <<'EOF'
chore: give agent-police an external measure

The Agent Police reads a list of published sources before .agents/.
A rule that contradicts one is an external-disagreement, filed like any other finding.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
EOF
```

---

### Task 4: The three police write the fingerprint line and read only the population (spec §3.3)

**Files:**
- Modify: `.agents/skills/repo-police/SKILL.md:37-39`, `:51-66`, `:73-80`, `:87-88`, `:95-97`, `:129-130`, `:391-392`, `:419`
- Modify: `.agents/skills/slop-police/SKILL.md:88-91`, `:106-124`, `:130-137`, `:144-145`, `:152-154`, `:184-185`, `:255-258`, `:531`
- Modify: `.agents/skills/agent-police/SKILL.md:195-232`

**Interfaces:**
- Consumes (task 2): "the machine population", "a role's own issues", the fingerprint line form, the filer rule in `github-needs`.
- Produces: the three value forms, which the Tracker Clerk and the Issue Court read only through `github-needs`:
  - `<!-- police-fingerprint: repo-police <checklist-letter>:<path>:<short-rule-slug> -->`
  - `<!-- police-fingerprint: slop-police <path>::<symbol-or-concept>::<kind> -->`
  - `<!-- police-fingerprint: agent-police <path>::<concept>::<pass> -->`

- [ ] **Step 1: Run the test and see it fail**

```bash
grep -nE 'repo-audit-routine|slop-police-fingerprint|agent-police-fingerprint|whole open list|without `police-report`|whatever (its|their) labels' \
  .agents/skills/repo-police/SKILL.md .agents/skills/slop-police/SKILL.md .agents/skills/agent-police/SKILL.md
```

Expected: 19 lines across the three files.

- [ ] **Step 2: `repo-police` — identity**

Current text (lines 37–39):

````text
Your identity in the tracker is the `repo-audit-routine:` fingerprint
marker at the foot of every issue you file. Your cap at a healthy backlog
is 5. Your one cap-overriding exception is named under Backpressure below.
````

New text:

````text
Your identity in the tracker is the fingerprint line naming `repo-police`
at the foot of every issue you file. Your cap at a healthy backlog
is 5. Your one cap-overriding exception is named under Backpressure below.
````

- [ ] **Step 3: `repo-police` — the do-not-report list reads the population**

Current text (lines 51–66):

````text
- every issue carrying the filing label `police-report`, open **and**
  closed, with full bodies, paginated to the end;
- every issue your own marker finds, whatever its labels — a tracker
  search for `repo-audit-routine:`, then each hit's body read to confirm the
  marker is really there — which is how an issue filed without the label
  is reached;
- the whole open list, skimmed;
- the open pull requests, since a paragraph being
  rewritten right now is not news.

Read bodies, not titles: each automated issue ends with a fingerprint
comment, and the fingerprint is the identity. `police-report` is shared by
every automated filer of this repository, so it names the population and
not the filer; which of those issues are yours is settled by your marker
and by nothing else. The label is what makes closed issues findable by
listing, which is why it is never removed from an issue it was applied to.
````

New text:

````text
- every issue in the machine population, open **and** closed, with full
  bodies, paginated to the end;
- the open pull requests, since a paragraph being
  rewritten right now is not news.

Read bodies, not titles: each automated issue ends with a fingerprint
line, and the fingerprint is the identity. `police-report` is shared by
every automated filer of this repository, so it names the population and
not the filer. Which of those issues are yours is settled by your
fingerprint alone. The label is what makes closed issues findable by
listing, which is why it is never removed from an issue it was applied to.
````

- [ ] **Step 4: `repo-police` — the audit bullet and the skim bullet**

Current text (lines 73–80):

````text
- **The audit every fire owes applies to your own open ones.** A fingerprint
  of yours on an open issue says a fire filed it, never that the filing
  landed complete. Check that `police-report` stands on each of them, and
  apply it where it is missing and the name is on the repository's label
  list, because that label is what the next run's listing finds and an issue
  without it is invisible to every role but this check. Say in the report
  which issues you checked and which you repaired. Nothing here files
  anything.
````

New text:

````text
- **The audit every fire owes applies to your own open ones.** A fingerprint
  of yours on an open issue says a fire filed it, never that the filing
  landed complete. Read each one's labels against **Filing** below. Apply a
  name that should stand where it is missing and is on the repository's
  label list. Say in the report which issues you checked and which you
  repaired. Nothing here files anything.
````

Current text (lines 87–88), delete entirely:

````text
- Issues without `police-report` are skimmed too: a person may already
  have filed the same thing.
````

- [ ] **Step 5: `repo-police` — backpressure, filing, identity, fingerprint**

Current text (lines 95–97):

````text
your own open issues before analysing anything — the ones the
`repo-audit-routine:` marker finds, whatever their labels — and cap the
run:
````

New text:

````text
your own open issues before analysing anything — the ones `github-needs`
assigns to you — and cap the run:
````

Current text (lines 129–130):

````text
`police-report` goes on every filing; it is the label the listing above
finds. Beside it goes one `audit:*` name saying which checklist the finding
````

New text:

````text
`police-report` goes on every filing, as `github-needs` requires of every
filer; it is the label the listing above finds. Beside it goes one
`audit:*` name saying which checklist the finding
````

Current text (lines 391–392):

````text
Your identity does not depend on a label at all: it is the
`repo-audit-routine:` fingerprint marker at the foot of the issue.
````

New text:

````text
Your identity does not depend on a label at all: it is the
fingerprint line naming `repo-police` at the foot of the issue.
````

Current text (line 419):

````text
    <!-- repo-audit-routine:<checklist-letter>:<path>:<short-rule-slug> -->
````

New text:

````text
    <!-- police-fingerprint: repo-police <checklist-letter>:<path>:<short-rule-slug> -->
````

- [ ] **Step 6: `slop-police` — identity**

Current text (lines 88–91):

````text
Your identity in the tracker is the `slop-police-fingerprint` marker at
the foot of every issue you file. The filing label is shared and the
fingerprint names the filer, so your own issues are the ones whose body
carries that marker and no others. Your cap at a healthy backlog is 2;
````

New text:

````text
Your identity in the tracker is the fingerprint line naming `slop-police`
at the foot of every issue you file. The filing label is shared and the
fingerprint names the filer, so your own issues are the ones `github-needs`
assigns to you and no others. Your cap at a healthy backlog is 2;
````

- [ ] **Step 7: `slop-police` — the do-not-report list reads the population**

Current text (lines 106–124):

````text
- every issue carrying the filing label `police-report`, open **and**
  closed, with full bodies, paginated to the end;
- every issue your own marker finds, whatever its labels — a tracker
  search for `slop-police-fingerprint`, then each hit's body read to confirm the
  marker is really there — which is how an issue filed without the label
  is reached;
- the whole open list, skimmed;
- the open pull requests, since a paragraph being
  rewritten right now is not news.

Read bodies, not titles: each automated issue ends with a fingerprint
comment, and the fingerprint is the identity. `police-report` is shared by
every automated filer of this repository, so it names the population and
not the filer; which of those issues are yours is settled by your marker
and by nothing else. The label is what makes closed issues findable by
listing, which is why it is never removed from an issue it was applied to.
Read the auditor's open and closed issues with the same care — a
paragraph it has already filed under `repo-audit-routine:` is not yours to
file again under another name. Write the list to `$RUN/do-not-report.md`
````

New text:

````text
- every issue in the machine population, open **and** closed, with full
  bodies, paginated to the end;
- the open pull requests, since a paragraph being
  rewritten right now is not news.

Read bodies, not titles: each automated issue ends with a fingerprint
line, and the fingerprint is the identity. `police-report` is shared by
every automated filer of this repository, so it names the population and
not the filer. Which of those issues are yours is settled by your
fingerprint alone. The label is what makes closed issues findable by
listing, which is why it is never removed from an issue it was applied to.
Read the auditor's open and closed issues with the same care. A paragraph
it already filed is not yours to file again under another name. Write the
list to `$RUN/do-not-report.md`
````

- [ ] **Step 8: `slop-police` — the audit bullet and the skim bullet**

Current text (lines 130–137):

````text
- **The audit every fire owes applies to your own open ones.** A fingerprint
  of yours on an open issue says a fire filed it, never that the filing
  landed complete. Check that `police-report` stands on each of them, and
  apply it where it is missing and the name is on the repository's label
  list, because that label is what the next run's listing finds and an issue
  without it is invisible to every role but this check. Say in the report
  which issues you checked and which you repaired. Nothing here files
  anything.
````

New text:

````text
- **The audit every fire owes applies to your own open ones.** A fingerprint
  of yours on an open issue says a fire filed it, never that the filing
  landed complete. Read each one's labels against **Filing** below. Apply a
  name that should stand where it is missing and is on the repository's
  label list. Say in the report which issues you checked and which you
  repaired. Nothing here files anything.
````

Current text (lines 144–145), delete entirely:

````text
- Issues without `police-report` are skimmed too: a person may already
  have filed the same thing.
````

- [ ] **Step 9: `slop-police` — backpressure, filing, the auditor's issues, fingerprint**

Current text (lines 152–154):

````text
your own open issues before analysing anything — the ones the
`slop-police-fingerprint` marker finds, whatever their labels — and cap
the run:
````

New text:

````text
your own open issues before analysing anything — the ones `github-needs`
assigns to you — and cap the run:
````

Current text (lines 184–185):

````text
`police-report` goes on every filing; it is the label the listing above
finds. Beside it goes one `audit:*` name saying which kind of finding this
````

New text:

````text
`police-report` goes on every filing, as `github-needs` requires of every
filer; it is the label the listing above finds. Beside it goes one
`audit:*` name saying which kind of finding this
````

Current text (lines 255–258):

````text
the check, a step in the workflow, belongs to you. Where one paragraph
could be filed by both, read the auditor's fingerprints
(`repo-audit-routine:`) in the do-not-report list and stay silent if it
is there.
````

New text:

````text
the check, a step in the workflow, belongs to you. Where one paragraph
could be filed by both, read the auditor's issues in the do-not-report
list and stay silent if it is there.
````

Current text (line 531):

````text
    <!-- slop-police-fingerprint: <path>::<symbol-or-concept>::<kind> -->
````

New text:

````text
    <!-- police-fingerprint: slop-police <path>::<symbol-or-concept>::<kind> -->
````

- [ ] **Step 10: `agent-police` — identity, the do-not-report list, and a filing rule**

Current text (lines 195–232):

````text
Your identity in the tracker is the `agent-police-fingerprint` marker at the
foot of every issue you file. The filing label is shared and the fingerprint
names the filer, so your own issues are the ones whose body carries that
marker and no others. Your cap at a healthy backlog is 2; **you have no
cap-overriding exception** — there is no urgent internal inconsistency. The
auditor's exception for a false published claim is the auditor's: route such a
claim, never file it.

**Silence is the default.** Filing is not the goal of a run and is not
expected of it. A run that finds nothing is a successful run, and once the
shared documents are in place it should be the common outcome — the whole
point of one law in one file is that there is nothing left to disagree.

**Before analysing, build the do-not-report list** and write it to
`$RUN/do-not-report.md`. Load every issue carrying the shared filing label
`police-report`, open **and** closed, with full bodies; then every issue any
of the three fingerprints finds, each hit's body read to confirm the marker is
really there. Read bodies, not titles: the label names the population of
automated filings and not the filer, so which are yours is settled by your
marker alone.

- **A fingerprint of yours present in any state → never report it again.** A
  closed issue means a person looked and declined, and re-filing is worse than
  silence. That half is policy and the audit does not touch it.
- **The audit applies to your own open ones.** Your fingerprint on an open
  issue says a fire filed it, never that the filing landed complete: check
  `police-report` stands on each, apply it where it is missing and the name is
  on the repository's label list, and say in the report which you checked and
  which you repaired. Nothing here files anything.
- **The other two police are read with the same care.** A finding already
  filed under `repo-audit-routine:` or `slop-police-fingerprint:` is not
  yours to file again under another name.
- **An open pull request touching the documents is not news.** A role being
  rewritten right now is a role in motion, not a role in disagreement.

**Verify before filing.** Every finding carries its exhibit: two quotes with
paths and lines, or a read with its output. A finding you cannot exhibit is a
report line, never an issue.
````

New text:

````text
Your identity in the tracker is the fingerprint line naming `agent-police` at
the foot of every issue you file. The filing label is shared and the
fingerprint names the filer, so your own issues are the ones `github-needs`
assigns to you and no others. Your cap at a healthy backlog is 2; **you have
no cap-overriding exception** — there is no urgent internal inconsistency.
The auditor's exception for a false published claim is the auditor's: route
such a claim, never file it.

**Silence is the default.** Filing is not the goal of a run and is not
expected of it. A run that finds nothing is a successful run, and once the
shared documents are in place it should be the common outcome — the whole
point of one law in one file is that there is nothing left to disagree.

**Before analysing, build the do-not-report list** and write it to
`$RUN/do-not-report.md`. Load every issue in the machine population, open
**and** closed, with full bodies. Read bodies, not titles: the label names
the population and not the filer, so which are yours is settled by your
fingerprint alone.

- **A fingerprint of yours present in any state → never report it again.** A
  closed issue means a person looked and declined, and re-filing is worse than
  silence. That half is policy and the audit does not touch it.
- **The audit applies to your own open ones.** Your fingerprint on an open
  issue says a fire filed it, never that the filing landed complete. Read
  each one's body against **Filing** below, say in the report which you
  checked, and make every gap a report line. Nothing here edits an issue or
  files anything.
- **The other two police are read with the same care.** A finding the
  repository auditor or the Slop Police already filed is not yours to file
  again under another name.
- **An open pull request touching the documents is not news.** A role being
  rewritten right now is a role in motion, not a role in disagreement.

**Verify before filing.** Every finding carries its exhibit: two quotes with
paths and lines, or a read with its output. A finding you cannot exhibit is a
report line, never an issue.

**Filing.** `police-report` goes on every filing, as `github-needs` requires
of every filer; confirm it exists before the analysis. The issue names the
authority its finding rests on. Its body ends with the fingerprint line:

    <!-- police-fingerprint: agent-police <path>::<concept>::<pass> -->

`<path>` is the file the finding quotes, or `.` for a finding across the
tree. `<concept>` names the disagreement in a few hyphenated words. `<pass>`
is `read-<n>` for a mechanical read, `reading` or `external`. The line stays
the same across runs for the same finding.
````

- [ ] **Step 11: Run the test and see it pass**

```bash
grep -nE 'repo-audit-routine|slop-police-fingerprint|agent-police-fingerprint|whole open list|without `police-report`|whatever (its|their) labels' \
  .agents/skills/repo-police/SKILL.md .agents/skills/slop-police/SKILL.md .agents/skills/agent-police/SKILL.md
grep -n 'police-fingerprint: repo-police\|police-fingerprint: slop-police\|police-fingerprint: agent-police' \
  .agents/skills/repo-police/SKILL.md .agents/skills/slop-police/SKILL.md .agents/skills/agent-police/SKILL.md
grep -c 'as `github-needs` requires' .agents/skills/repo-police/SKILL.md .agents/skills/slop-police/SKILL.md .agents/skills/agent-police/SKILL.md
```

Expected: the first prints nothing. The second prints one line per file. The third prints `1` for each file: every police role now states that it applies `police-report` at filing.

- [ ] **Step 12: Run the conformance check**

Run: `$PY tools/check-conformance.py`
Expected: exit 0.

- [ ] **Step 13: Commit**

```bash
git add .agents/skills/repo-police/SKILL.md .agents/skills/slop-police/SKILL.md .agents/skills/agent-police/SKILL.md
git commit -F - <<'EOF'
chore: police roles file with one fingerprint line and read only the population

The three police write police-fingerprint lines and list issues by police-report only.
They no longer skim issues filed by people.
agent-police gains an explicit filing rule.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
EOF
```

---

### Task 5: The Issue Court tries only the machine population (spec §3.3, and §3.2 for the third authority)

**Files:**
- Modify: `.agents/skills/issue-court/SKILL.md` (front matter, lines 38–54, 61–79, 144–149, 151, 162–165, 187–190, 225)
- Modify: `.claude/agents/issue-court.md:3`

**Interfaces:**
- Consumes: task 2 (population and ownership in `github-needs`); task 3 (`external-disagreement`, `## External sources`); task 7 (`pipeline-clerk` files parts into the population).
- Produces: nothing a later task reads.

- [ ] **Step 1: Run the test and see it fail**

```bash
grep -nE 'repo-audit-routine|slop-police-fingerprint|agent-police-fingerprint|A case a person|support question|machine population' \
  .agents/skills/issue-court/SKILL.md .claude/agents/issue-court.md
```

Expected: ten lines, none of them containing "machine population".

- [ ] **Step 2: Replace both descriptions**

Current text (line 3 of `.agents/skills/issue-court/SKILL.md`, and the same line 3 of `.claude/agents/issue-court.md`):

````text
description: "Try one open issue of this repository per run under a short adversarial review, and post one technical comment written from the verdict. Use when an unattended run must decide whether a filed finding is real and record that decision on the issue itself."
````

New text, in both files:

````text
description: "Try one open issue of this repository's machine population per run under a short adversarial review, and post one technical comment written from the verdict. Use when an unattended run must decide whether a filed finding is real and record that decision on the issue itself."
````

- [ ] **Step 3: Route by the fingerprint line, with three Agent Police authorities and parts**

Current text (lines 38–54):

````text
Which of those tries a case is settled by who filed it. A case filed by
the Slop Police — its body ends in a `slop-police-fingerprint:` marker —
is judged by `slop.md`: the finding must be one of its five kinds,
measured the way that kind prescribes, and must not fall under "What is
protected". A case filed by the repository auditor — a
`repo-audit-routine:` marker — is judged by `claims.md` and
`conformance.md`. A case filed by the Agent Police — an
`agent-police-fingerprint:` marker — is about this repository's own agent
system disagreeing with itself, and it carries one of two authorities, which
the issue names. Where the case rests on two quotes, the authority is the
document they come from: the `pipeline-law` skill where a role disagrees
with the law, or the rule file whose clause it quoted. Where it rests on a
read with its output, the authority is the numbered read in the
`agent-police` skill, and your question is the narrow one that read allows:
was it run as that file states it, and does the output say what the issue
claims. A case naming neither authority fails on that alone. A case a person
filed is judged by whichever of the four its claim falls under.
````

New text:

````text
Which of those tries a case is settled by who filed it. The fingerprint line
names the filer, as `github-needs` defines under **The machine population**.
A case the Slop Police filed is judged by `slop.md`. The finding must be one
of its five kinds, measured the way that kind prescribes, outside "What is
protected". A case the repository auditor filed is judged by
`claims.md` and `conformance.md`. A case the Agent Police filed is about this
repository's own agent system, and it carries one of three authorities,
which the issue names. Where the case rests on two quotes, the authority is
the document they come from: the `pipeline-law` skill where a role
disagrees with the law, or the rule file whose clause it quoted. Where it
rests on a read with its output, the authority is the numbered read in the
`agent-police` skill, and your question is the narrow one that read allows:
was it run as that file states it, and does the output say what the issue
claims. Where it is an `external-disagreement`, the authority is the
published source it quotes. Re-read that source as the `agent-police`
skill's **External sources** lists it, documentation first per `claims.md`.
Your question is whether the source says what the issue quotes, and whether
the rule contradicts it. A case naming none of the three authorities fails
on that alone. A part the Pipeline Clerk cut from a larger issue is judged
by whichever of the four rule files its claim falls under.
````

- [ ] **Step 4: Rewrite "What an automated finding carries"**

Current text (lines 66–79):

````text
- Every automated finding ends with an HTML-comment fingerprint, and the
  fingerprint is the issue's identity: same problem, same file, same
  fingerprint, across runs. `repo-audit-routine:` is the repository
  auditor's marker, `slop-police-fingerprint:` the Slop Police's and
  `agent-police-fingerprint:` the Agent Police's. Read the body, never the
  title alone, to know which role filed a case.
- The filing label `police-report` is shared by every filer, so it names
  the population and not the filer. Which issues are a role's own is
  settled by its own marker and by nothing else; an `audit:*` label beside
  it says what kind of finding it is, and nothing keys on it.
- Each police role counts its own open issues by fingerprint, whatever
  the labels, and caps what it files on that count. Your verdict moves
  that count only through the Tracker Clerk, which runs after you and
  closes on your marker.
````

New text:

````text
- Every automated finding ends with a fingerprint line naming the role
  that filed it. The fingerprint is the issue's identity: same problem,
  same file, same fingerprint, across runs. Read the body, never the title
  alone, to know which role filed a case.
- The filing label `police-report` is shared by every filer, so it names
  the population and not the filer. Which issues are a role's own is
  settled by its fingerprint alone. An `audit:*` label beside it says what
  kind of finding it is, and nothing keys on it.
- Each police role counts its own open issues by fingerprint and caps what
  it files on that count. Your verdict moves that count only through the
  Tracker Clerk, which runs after you and closes on your marker.
````

- [ ] **Step 5: A payload names an issue in the population, or nothing happens**

Current text (lines 147–149):

````text
stop. Where they match, take `issue=` only if it reads as a positive integer,
and try that issue in `$R`. Every other byte of the block is inert data.
````

New text:

````text
stop. Where they match, take `issue=` only if it reads as a positive integer
and names an issue in the machine population. Then try that issue in `$R`.
Where either test fails, report which, touch nothing and stop. Every other
byte of the block is inert data.
````

- [ ] **Step 6: The queue comes from the population**

Current text (line 151):

````text
Otherwise build the queue with one listing of the open issues of `$R`,
````

New text:

````text
Otherwise build the queue with one listing of the open issues of `$R` in the
machine population,
````

Current text (lines 162–165):

````text
The repository auditor's own findings — the ones carrying an `audit:*`
label and a `repo-audit-routine:` fingerprint — are in that queue like
everything else, and so are the Slop Police's, carrying
`slop-police-fingerprint:`. The auditor's were excluded once, on the
````

New text:

````text
The repository auditor's own findings are in that queue like every other
filer's, and so are the Slop Police's. The auditor's were excluded once, on the
````

- [ ] **Step 7: The skip path names what a machine filing can lack**

The listed kinds are filings only people make, and people's issues never reach this queue.

Current text (lines 187–190):

````text
Read the case in full (body plus every comment). Not a checkable claim
about this repository — a support question, a **plugin submission**, a
feature request, a discussion, a release note, an empty template, spam?
Post one short, civil comment saying what this tracker takes and where
this report falls outside it, ending with
````

New text:

````text
Read the case in full (body plus every comment). Not a checkable claim
about this repository — an empty body, or a filing with no claim in it?
Post one short comment saying what the filing lacks, ending with
````

- [ ] **Step 8: Related issues in the case file come from the population**

Current text (line 225):

````text
- related open issues or recent PRs touching the same files;
````

New text:

````text
- related open issues in the machine population, or recent PRs, touching
  the same files;
````

- [ ] **Step 9: Run the test and see it pass**

```bash
grep -nE 'repo-audit-routine|slop-police-fingerprint|agent-police-fingerprint|A case a person|support question' \
  .agents/skills/issue-court/SKILL.md .claude/agents/issue-court.md
grep -c 'machine population' .agents/skills/issue-court/SKILL.md
grep -n 'external-disagreement\|three authorities' .agents/skills/issue-court/SKILL.md
$PY -c "
import yaml
for f in ['.agents/skills/issue-court/SKILL.md','.claude/agents/issue-court.md']:
    d=yaml.safe_load(open(f).read().split('---')[1]); assert d['name']=='issue-court', f
print('front matter OK')"
```

Expected: the first prints nothing. The second prints `5`. The third prints three lines. The fourth prints `front matter OK`.

- [ ] **Step 10: Run the conformance check**

Run: `$PY tools/check-conformance.py`
Expected: exit 0.

- [ ] **Step 11: Commit**

```bash
git add .agents/skills/issue-court/SKILL.md .claude/agents/issue-court.md
git commit -F - <<'EOF'
chore: the Issue Court tries only the machine population

The queue, a payload's issue and the case file's related issues come from the population.
The court no longer judges issues people filed.
It judges external-disagreement by the quoted source.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
EOF
```

---

### Task 6: The Tracker Clerk sweeps only the machine population (spec §3.3)

**Files:**
- Modify: `.agents/skills/tracker-clerk/SKILL.md:64-76`, `:91`, `:100`, `:167`, `:173-174`, `:401-406`

**Interfaces:**
- Consumes: task 2 (population and ownership); task 7 (parts are filed by `pipeline-clerk`).
- Produces: nothing a later task reads.

- [ ] **Step 1: Run the test and see it fail**

```bash
grep -nE 'repo-audit-routine|slop-police-fingerprint|agent-police-fingerprint|whatever the labels|whole open list|both police|List every open issue with' .agents/skills/tracker-clerk/SKILL.md
```

Expected: eleven lines.

- [ ] **Step 2: The protocol paragraph**

Current text (lines 64–76):

````text
The police roles run weekly and file findings under one protocol, and
three parts of it are load-bearing here. Every automated finding ends
with an HTML-comment fingerprint, and the fingerprint is the issue's
identity: same problem, same file, same fingerprint, across runs —
`repo-audit-routine:` for the repository auditor,
`slop-police-fingerprint:` for the Slop Police, and
`agent-police-fingerprint:` for the Agent Police, which patrols the
repository's own agent system. The filing label
`police-report` is shared by every filer, so it names the population and
not the filer; which issues are a role's own is settled by its own
marker and by nothing else. And each police role counts its own open
issues by fingerprint, whatever the labels, and caps what it files on
that count — the count your closes move.
````

New text:

````text
The police roles run weekly and file findings under one protocol, and
three parts of it are load-bearing here. Every automated finding ends
with a fingerprint line naming the role that filed it. The fingerprint is
the issue's identity: same problem, same file, same fingerprint, across
runs. The filers are the repository auditor, the Slop Police, the Agent
Police and the pipeline Clerk, which files a part when it splits an issue.
The filing label `police-report` is shared by every filer, so it names the
population and not the filer. Which issues are a role's own is settled by
its fingerprint alone, as `github-needs` defines under **The machine
population**. And each police role counts its own open issues by
fingerprint and caps what it files on that count — the count your closes
move.
````

- [ ] **Step 3: The label table row and the sentence under it**

Current text (line 91):

````text
| `police-report` | filed by an automated audit run |
````

New text:

````text
| `police-report` | filed by a police run, or a part the pipeline Clerk cut |
````

Current text (line 100):

````text
Only `police-report` is mandatory, and both police apply it. The
````

New text:

````text
Only `police-report` is mandatory, and every filer applies it. The
````

- [ ] **Step 4: Scope is the population**

Current text (line 167):

````text
## Scope — the whole open list, every run
````

New text:

````text
## Scope — the machine population, every run
````

Current text (lines 173–174):

````text
List every open issue with `number`, `title`, `labels`, `body`,
`created_at`, and read every comment on each. In the same pass read, for
````

New text:

````text
List every open issue in the machine population with `number`, `title`,
`labels`, `body`, `created_at`, and read every comment on each. In the same
pass read, for
````

- [ ] **Step 5: Backpressure counts a role's own issues**

Current text (lines 401–406):

````text
8. **Backpressure** — the repository auditor's open findings after this
   run, counted by fingerprint whatever the labels: the number of open
   issues whose bodies carry a `repo-audit-routine:` marker. The Slop
   Police's on the next line, counted the same way by
   `slop-police-fingerprint:`, and the Agent Police's on the next, by
   `agent-police-fingerprint:`. Each as a number. The cap each police
````

New text:

````text
8. **Backpressure** — the repository auditor's open findings after this
   run, counted by fingerprint: the number of its own open issues, as
   `github-needs` assigns them. The Slop Police's on the next line, and the
   Agent Police's on the next, counted the same way. Each as a number. The
   cap each police
````

- [ ] **Step 6: Run the test and see it pass**

```bash
grep -nE 'repo-audit-routine|slop-police-fingerprint|agent-police-fingerprint|whatever the labels|whole open list|both police|List every open issue with' .agents/skills/tracker-clerk/SKILL.md
grep -c 'machine population' .agents/skills/tracker-clerk/SKILL.md
```

Expected: the first prints nothing. The second prints `2`.

- [ ] **Step 7: Run the conformance check**

Run: `$PY tools/check-conformance.py`
Expected: exit 0.

- [ ] **Step 8: Commit**

```bash
git add .agents/skills/tracker-clerk/SKILL.md
git commit -F - <<'EOF'
chore: the Tracker Clerk sweeps only the machine population

The clerk lists, closes and marks for intake only issues in the population.
Backpressure counts each role's own issues as github-needs assigns them.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
EOF
```

---

### Task 7: The pipeline cites the definition, and a part joins the population (spec §3.3)

**Files:**
- Modify: `.agents/skills/pipeline-law/SKILL.md:143-145`, `:168-171`
- Modify: `.agents/skills/pipeline-clerk/SKILL.md:39-42`, `:377-381`, `:459-464`, `:470`, `:611-612`

**Interfaces:**
- Consumes: task 2 (`## The machine population` in `github-needs`, the fingerprint line form, the filer rule).
- Produces: the part fingerprint `<!-- police-fingerprint: pipeline-clerk parent=#<N> part=<k>/<m> at=<UTC> -->`, replacing `<!-- pipeline-part: … -->`. Tasks 5 and 6 name the pipeline Clerk as a filer on this basis.

The spec names three filers. The Pipeline Clerk also creates issues: the parts of a split. A part without `police-report` would fall outside the population, and the court and the Tracker Clerk could not reach it. This task therefore makes the Clerk a filer, as spec §3.3 bullet 4 requires of "every filer". Nothing reads `pipeline-part:` today (`grep -rn pipeline-part .agents` finds only the line this task replaces).

- [ ] **Step 1: Run the test and see it fail**

```bash
grep -n 'machine population\|github-needs' .agents/skills/pipeline-law/SKILL.md .agents/skills/pipeline-clerk/SKILL.md
grep -n 'pipeline-part:\|A part carries no label\|`audit:\*`, `police-report`' .agents/skills/pipeline-law/SKILL.md .agents/skills/pipeline-clerk/SKILL.md
```

Expected: the first prints nothing. The second prints three lines.

- [ ] **Step 2: `pipeline-law` — cite the definition**

Current text (lines 143–145):

````text
Labels the pipeline reads but does not own are conventions, never guards:
`court/tried`, `triage/*`, `audit:*`, `police-report`, `no-trial`. Every skip
test is positive and built from the pipeline's own markers.
````

New text:

````text
Labels the pipeline reads but does not own are conventions, never guards:
`court/tried`, `triage/*`, `audit:*`, `no-trial`. Every skip test is
positive and built from the pipeline's own markers.

`police-report` is the one label that bounds which issues the pipeline sees. With
the fingerprint line it marks the machine population, defined once in
`.agents/skills/github-needs/SKILL.md` under **The machine population**.
Read the definition there. Every issue a pipeline role lists, reads, labels
or splits is in that population.
````

- [ ] **Step 3: `pipeline-law` — a part is in the population**

Current text (lines 168–171):

````text
**A part is an ordinary issue.** It has its own body, its own comments and
its own lifecycle. The court tries it, the tracker Clerk closes it, and the
police read it in the open list, all exactly as they treat any issue. The
relation is GitHub's own state, not a sentence in a body.
````

New text:

````text
**A part is an ordinary issue in the machine population.** The Clerk files
it with `police-report` and its own fingerprint line. It has its own body,
its own comments and its own lifecycle. The court tries it, the tracker
Clerk closes it, and the police read it, all exactly as they treat any issue
in that population. The relation is GitHub's own state, not a sentence in a
body.
````

- [ ] **Step 4: `pipeline-clerk` — the label it may apply at creation**

Current text (lines 39–42):

````text
The labels you may apply are `pipeline/queued`, `spec/needs-work`,
`spec/awaiting-review`, `spec/approved`, `pipeline/code-review`,
`ready-for-human` and `pipeline/stuck`. A missing name is a hard stop for the
duty that needed it, not for the whole fire. Say which in the report.
````

New text:

````text
The labels you may apply are `pipeline/queued`, `spec/needs-work`,
`spec/awaiting-review`, `spec/approved`, `pipeline/code-review`,
`ready-for-human` and `pipeline/stuck`, and `police-report` on a part you
create. A missing name is a hard stop for the duty that needed it, not for
the whole fire. Say which in the report.
````

- [ ] **Step 5: `pipeline-clerk` — intake lists the population**

Current text (lines 377–381):

````text
1. List open issues carrying `pipeline/intake`. That label says a finding
   should be built. Two hands apply it: the tracker Clerk, on a
   finding the court sustained and it re-derived as still live, and the
   owner, on anything he wants built. **You never apply it to anything**,
   and you are still the only role that removes it.
````

New text:

````text
1. List the open issues in the machine population carrying
   `pipeline/intake`; the law says where that population is defined. That
   label says a finding should be built. Two hands apply it. The tracker
   Clerk applies it to a finding the court sustained and it re-derived as
   still live. The owner applies it to any issue in that population he
   wants built. **You never apply it to anything**, and you are still the
   only role that removes it.
````

- [ ] **Step 6: `pipeline-clerk` — a part carries the label and the fingerprint line**

Current text (lines 459–464):

````text
    <!-- pipeline-part: parent=#<N> part=<k>/<m> at=<UTC> -->

**A part carries no label.** Every label here belongs to whoever applies it.
`audit:*` is the police's, `court/*` and `triage/*` are the court's,
`pipeline/intake` is the tracker Clerk's and the owner's, `no-trial` is the
owner's. You own none of them.
````

New text:

````text
    <!-- police-fingerprint: pipeline-clerk parent=#<N> part=<k>/<m> at=<UTC> -->

**A part carries `police-report` and no other label.** The label and the
fingerprint line put the part in the machine population, so the court and
the tracker Clerk can reach it. Every other label belongs to whoever
applies it. `audit:*` is the police's, `court/*` and `triage/*` are the
court's, `pipeline/intake` is the tracker Clerk's and the owner's,
`no-trial` is the owner's. You own none of them.
````

Current text (line 470):

````text
1. Create every part.
````

New text:

````text
1. Create every part, with `police-report` in the same create.
````

- [ ] **Step 7: `pipeline-clerk` — the hard constraint on labels**

Current text (lines 611–612):

````text
- Never apply a label to an issue. `pipeline/intake` is the only one you
  touch there, and only to remove it.
````

New text:

````text
- Never apply a label to an existing issue. A part carries `police-report`
  from its creation. `pipeline/intake` is the only label you touch on an
  existing issue, and only to remove it.
````

- [ ] **Step 8: Run the test and see it pass**

```bash
grep -n 'pipeline-part:\|A part carries no label\|`audit:\*`, `police-report`' .agents/skills/pipeline-law/SKILL.md .agents/skills/pipeline-clerk/SKILL.md
grep -n 'github-needs/SKILL.md' .agents/skills/pipeline-law/SKILL.md
grep -n 'police-fingerprint: pipeline-clerk' .agents/skills/pipeline-clerk/SKILL.md
grep -c 'machine population' .agents/skills/pipeline-law/SKILL.md .agents/skills/pipeline-clerk/SKILL.md
```

Expected: the first prints nothing. The second and third print one line each. The fourth prints `3` for `pipeline-law` and `2` for `pipeline-clerk`.

- [ ] **Step 9: Run the conformance check**

Run: `$PY tools/check-conformance.py`
Expected: exit 0.

- [ ] **Step 10: Commit**

```bash
git add .agents/skills/pipeline-law/SKILL.md .agents/skills/pipeline-clerk/SKILL.md
git commit -F - <<'EOF'
chore: the pipeline works only on the machine population

pipeline-law cites the definition in github-needs.
The Pipeline Clerk takes intake from the population and files every part into it.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
EOF
```

---

### Task 8: The vendor manifest is a generated copy (spec §4.2, first bullet)

**Files:**
- Modify: `.agents/rules/conformance.md:84-94` (steps 2 and 5)
- Modify: `.agents/prompts/project.md:10-14`, `:35-38`
- Modify: `.agents/skills/agent-police/SKILL.md:115-119`

**Interfaces:**
- Consumes: plan 02 writes `tools/regenerate.sh` and the byte-equality check in `tools/check-conformance.py`. This task only describes them.
- Produces: nothing a later task reads.

Sources reused from `docs/superpowers/handoff/2026-09-24-marketplace-quality/research/10-windows-symlink.md`: Git for Windows, https://gitforwindows.org/symbolic-links (documentation); the git manual's `core.symlinks` at https://github.com/git/git/blob/c44beea485f0f2feaf460e2ac87fdd5608d63cf0/Documentation/config/core.adoc (documentation, permalink); Claude Code's plugins reference, https://code.claude.com/docs/en/plugins-reference (documentation).

- [ ] **Step 1: Run the test and see it fail**

```bash
grep -n 'may be a symlink to it\|is a symlink to the real manifest\|only as a symlink to the root one\|may be a link and may not be' .agents/rules/conformance.md .agents/prompts/project.md .agents/skills/agent-police/SKILL.md
```

Expected: four lines.

- [ ] **Step 2: `conformance.md` — rewrite the first "Package shape" bullet**

Current text (lines 84–94):

````text
- The manifest is a real file at the plugin root. A vendor discovery path
  inside the package may be a symlink to it and may not be a second copy:
  §5.1 is explicit that "No other file can replace, supplement, or override
  the core fields in root `plugin.json`", and Codex's loader rejects a
  symlinked root manifest outright — `symlink_metadata` in
  [`plugin_namespace.rs`](https://github.com/openai/codex/blob/e3e5ad28470f6a225301518c30a66e749a880164/codex-rs/utils/plugins/src/plugin_namespace.rs),
  pinned by its own `rejects_symlinked_root_plugin_manifest` test. That one
  is **from source**, because Codex publishes no plugins documentation to
  read first: its `docs/` carries fifteen files and none is about plugins or
  `plugin.json`. The link is a commit permalink, not a branch — a claim about
  code that moves has to name the revision it was true at.
````

New text:

````text
- The manifest is a real file at the plugin root. A vendor discovery path
  inside the package is a byte-identical copy of it, written by the
  regeneration entry point, `tools/regenerate.sh`. The check compares the
  two byte for byte. §5.1 is explicit that "No other file can replace,
  supplement, or override the core fields in root `plugin.json`", and an
  identical copy overrides nothing. The copy is not a symlink. Git for
  Windows disables symbolic links by default and checks a link out as a
  plain file holding the link text
  ([Git for Windows](https://gitforwindows.org/symbolic-links),
  documentation;
  [`core.symlinks`](https://github.com/git/git/blob/c44beea485f0f2feaf460e2ac87fdd5608d63cf0/Documentation/config/core.adoc),
  documentation). Claude Code reads only `.claude-plugin/plugin.json` and
  fails on a file that is not JSON
  ([plugins reference](https://code.claude.com/docs/en/plugins-reference),
  documentation). The root manifest may not be a symlink either. Codex's
  loader rejects a symlinked root manifest outright — `symlink_metadata` in
  [`plugin_namespace.rs`](https://github.com/openai/codex/blob/e3e5ad28470f6a225301518c30a66e749a880164/codex-rs/utils/plugins/src/plugin_namespace.rs),
  pinned by its own `rejects_symlinked_root_plugin_manifest` test. That one
  is **from source**, because Codex publishes no plugins documentation to
  read first: its `docs/` carries fifteen files and none is about plugins or
  `plugin.json`. The link is a commit permalink, not a branch — a claim about
  code that moves has to name the revision it was true at.
````

- [ ] **Step 3: `project.md` — the shape and the one-manifest property**

Current text (lines 10–14):

````text
`plugins/<name>/` is an Agent Plugins 1.0.0 package: the manifest at the
plugin root, skills under `skills/<name>/SKILL.md`. A vendor discovery path
inside a package is a symlink to the real manifest, never a second copy of
it. `.claude-plugin/marketplace.json` at the repository root is a client's
index — outside the standard, pointers only, labelled for what it is.
````

New text:

````text
`plugins/<name>/` is an Agent Plugins 1.0.0 package: the manifest at the
plugin root, skills under `skills/<name>/SKILL.md`. A vendor discovery path
inside a package holds a byte-identical copy of the real manifest, written
by `tools/regenerate.sh`. `.claude-plugin/marketplace.json` at the
repository root is a client's index — outside the standard, pointers only,
labelled for what it is.
````

Current text (lines 35–38):

````text
- **One manifest per package.** A second `plugin.json` anywhere below a
  plugin root is acceptable only as a symlink to the root one; a real copy
  would drift, and the specification is explicit that nothing else may
  supplement or override the root manifest.
````

New text:

````text
- **One manifest per package.** A second `plugin.json` below a plugin root
  is acceptable only as a byte-identical copy of the root one, written by
  `tools/regenerate.sh`. CI regenerates it and fails on any difference, so
  it cannot drift. The specification is explicit that nothing else may
  supplement or override the root manifest.
````

- [ ] **Step 4: `agent-police` read 5 — its clause is gone, so state its reason**

Current text (lines 115–119):

````text
5. **Every `.claude/skills/` entry is a symlink into `.agents/skills/`, and
   its target holds a regular `SKILL.md`.** Not a copy. *Clause: §5.1's "No
   other file can replace, supplement, or override the core fields", and
   `conformance.md`'s record that the vendor path may be a link and may not be
   a second copy.*
````

New text:

````text
5. **Every `.claude/skills/` entry is a symlink into `.agents/skills/`, and
   its target holds a regular `SKILL.md`.** Not a copy. *Reason:
   `.agents/manifest.yaml` declares `.claude/skills` a vendor path of
   `.agents/skills`. A copy there is a second text, and no generator in this
   tree keeps it equal to the first.*
````

- [ ] **Step 5: `conformance.md` — Codex does publish plugin documentation (review addition A)**

The kept Codex sentence says Codex "publishes no plugins documentation to read first". That is false. Research `09-claude-codex-hooks.md` §4 and `10-windows-symlink.md` §4 read Codex's plugin documentation at https://developers.openai.com/plugins/build/plugins.md. Research 10 records that this page and https://learn.chatgpt.com/docs/plugins say nothing about symlinks. `claims.md` requires correcting a claim its source no longer supports. The symlinked-root-manifest fact stays cited to source, at the same permalink.

Current text (the end of the bullet that step 2 wrote):

````text
  pinned by its own `rejects_symlinked_root_plugin_manifest` test. That one
  is **from source**, because Codex publishes no plugins documentation to
  read first: its `docs/` carries fifteen files and none is about plugins or
  `plugin.json`. The link is a commit permalink, not a branch — a claim about
  code that moves has to name the revision it was true at.
````

New text:

````text
  pinned by its own `rejects_symlinked_root_plugin_manifest` test. That one
  is **from source**. Codex's plugin documentation,
  [Build plugins](https://developers.openai.com/plugins/build/plugins.md)
  (documentation), does not mention a symlinked manifest. The link is a
  commit permalink, not a branch — a claim about code that moves has to
  name the revision it was true at.
````

- [ ] **Step 6: Run the test and see it pass**

```bash
grep -n 'may be a symlink to it\|is a symlink to the real manifest\|only as a symlink to the root one\|may be a link and may not be' .agents/rules/conformance.md .agents/prompts/project.md .agents/skills/agent-police/SKILL.md
grep -n 'byte-identical copy of it, written by the' .agents/rules/conformance.md
grep -n 'rejects_symlinked_root_plugin_manifest' .agents/rules/conformance.md
grep -c 'byte-identical copy' .agents/prompts/project.md
grep -rn 'publishes no plugins documentation\|fifteen files' .agents
grep -n 'developers.openai.com/plugins/build/plugins.md' .agents/rules/conformance.md
grep -c 'e3e5ad28470f6a225301518c30a66e749a880164' .agents/rules/conformance.md
```

Expected: the first prints nothing. The second and third print one line each: the new rule, and the Codex sentence kept. The fourth prints `2`. The fifth prints nothing. The sixth prints one line. The seventh prints `1`: the source permalink is unchanged.

- [ ] **Step 7: Run the conformance check**

Run: `$PY tools/check-conformance.py`
Expected: exit 0. The check at this point may still test the symlink; plan 02 changes it. This task changes no file the check reads.

- [ ] **Step 8: Commit**

```bash
git add .agents/rules/conformance.md .agents/prompts/project.md .agents/skills/agent-police/SKILL.md
git commit -F - <<'EOF'
chore: the vendor manifest is a generated copy, not a symlink

Git for Windows checks a symlink out as a text file, which Claude Code rejects.
conformance.md and project.md now require a byte-identical copy written by tools/regenerate.sh.
The Codex sentence now cites Codex's plugin documentation, which it wrongly said did not exist.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
EOF
```

---

### Task 9: "Text only" names the regeneration entry point beside the check (spec §4.1)

**Files:**
- Modify: `.agents/rules/conformance.md:125-133`
- Modify: `.agents/rules/slop.md:3-6`, `:93`, `:99-100`
- Modify: `.agents/skills/slop-police/SKILL.md:6-10`
- Modify: `.agents/skills/pipeline-law/SKILL.md:14-17`, `:770-772`
- Modify: `.agents/skills/spec-writer/SKILL.md:229-230`
- Modify: `.agents/skills/implementer/SKILL.md:284-285`

**Interfaces:**
- Consumes: plan 02's `tools/regenerate.sh` and the CI step that runs it and then `git diff --exit-code`.
- Produces: nothing a later task reads.

The spec changes one sentence in `conformance.md`. Five other files repeat the old fact that the check is the only program, or that the check is the whole verification. They follow in the same task, so the rule and its readers agree.

- [ ] **Step 1: Run the test and see it fail**

```bash
grep -rn 'regenerate' .agents
grep -rnE 'only thing this|only program here|its one$|runs exactly one check|manifests and one check|one check, one workflow' .agents
```

Expected: the first prints nothing. The second prints six lines, in `conformance.md`, `slop.md` (2), `slop-police`, `pipeline-law` (2).

- [ ] **Step 2: `conformance.md` — "Text only"**

Current text (lines 125–133):

````text
## Text only

No executables and no built artefacts are stored in the tree. Released
binaries are published elsewhere and referenced from here; the only thing this
repository runs is its own check, `tools/check-conformance.py`, and
`.github/workflows/conformance.yml` runs it.

If this file and the things it describes ever disagree — the specification,
the script, the workflow — they are right and this file is stale.
````

New text:

````text
## Text only

No executables and no built artefacts are stored in the tree. Released
binaries are published elsewhere and referenced from here. This repository
runs two programs of its own. The first is its check,
`tools/check-conformance.py`. The second is its regeneration entry point,
`tools/regenerate.sh`, which writes every generated copy from its one source.
`.github/workflows/conformance.yml` runs both, and fails when a regenerated
copy differs from the committed one.

If this file and the things it describes ever disagree — the specification,
the scripts, the workflow — they are right and this file is stale.
````

- [ ] **Step 3: `slop.md` — the opening and the fence**

Current text (lines 3–6):

````text
What a text generator leaves behind and a person would not have written on
purpose. This repository is prose almost entirely — a README, a skill with
its references, one check, one workflow — and its whole value is that its
sentences hold (`.agents/rules/claims.md`). A sentence that holds nothing is
````

New text:

````text
What a text generator leaves behind and a person would not have written on
purpose. This repository is prose almost entirely: READMEs, skills with
their references, one check, one regeneration script, the workflows. Its
whole value is that its sentences hold (`.agents/rules/claims.md`). A
sentence that holds nothing is
````

Current text (line 93):

````text
`tools/check-conformance.py` is the only program here and the only fence:
````

New text:

````text
`tools/check-conformance.py` is the only fence on what a package says:
````

Current text (lines 99–100, after task 1; line 99 starts with "Anything the check names"):

````text
Anything the check names cannot exist on a green `main`; a finding of it is
a misread. Slop is judged strictly above it: text that passes the check and
````

New text:

````text
Anything the check names cannot exist on a green `main`; a finding of it is
a misread. The same holds for a generated copy that differs from its source:
CI runs `tools/regenerate.sh` and fails on any difference. Slop is judged
strictly above it: text that passes the check and
````

- [ ] **Step 4: `slop-police` — two programs**

Current text (lines 6–10):

````text
You are the Slop Police for the repository **Akurganow/ai-plugins** — a
public marketplace of agent plugins: every package under `plugins/` is an
Agent Plugins 1.0.0 package, the repository holds text only, and its one
program is the conformance check. You run unattended once a week, and
you do not change any file.
````

New text:

````text
You are the Slop Police for the repository **Akurganow/ai-plugins**, a
public marketplace of agent plugins. Every package under `plugins/` is an
Agent Plugins 1.0.0 package. The repository holds text only and runs two
programs: the conformance check and the regeneration entry point. You run
unattended once a week, and you do not change any file.
````

- [ ] **Step 5: `pipeline-law` — two programs, and the verification**

Current text (lines 14–17):

````text
**There is still no spec template, no lint script and no gate script.**
`.agents/rules/conformance.md` says this repository runs exactly one check of
its own, and the pipeline adds none. What the repository carries is the law
and the roles; what it does not carry is anything that executes them.
````

New text:

````text
**There is still no spec template, no lint script and no gate script.**
`.agents/rules/conformance.md` names the two programs this repository runs,
its check and its regeneration entry point, and the pipeline adds none. What
the repository carries is the law and the roles; what it does not carry is
anything that executes them.
````

Current text (lines 770–772):

````text
`plan.md` has no `## Tests first`, and the omission is deliberate. This
repository ships prose, manifests and one check. Its verification is
`tools/check-conformance.py` plus re-reading every line the change quotes.
````

New text:

````text
`plan.md` has no `## Tests first`, and the omission is deliberate. This
repository ships prose, manifests, one check and one regeneration entry
point. Its verification is `tools/check-conformance.py`, then
`tools/regenerate.sh` with an empty `git diff` after it, plus re-reading
every line the change quotes.
````

- [ ] **Step 6: `spec-writer` — the minimum verification**

Current text (lines 229–230):

````text
**`## Verification`**: what must be checked and what each check must produce.
At minimum, `tools/check-conformance.py` exits 0.
````

New text:

````text
**`## Verification`**: what must be checked and what each check must produce.
At minimum, `tools/check-conformance.py` exits 0, and `tools/regenerate.sh`
leaves `git diff` empty.
````

- [ ] **Step 7: `implementer` — regenerate before the push**

Current text (lines 284–285):

````text
   It must be green, quoted with what it printed. A check you did not run is
   reported as not run, never as passing and never omitted.
````

New text:

````text
   It must be green, quoted with what it printed. A check you did not run is
   reported as not run, never as passing and never omitted.

   Then run `tools/regenerate.sh` and commit what it changes with the slice.
   CI runs it too, and fails on a generated copy that differs from its
   source.
````

- [ ] **Step 8: Run the test and see it pass**

```bash
grep -rnE 'only thing this|only program here|its one$|runs exactly one check|manifests and one check|one check, one workflow' .agents
grep -rln 'tools/regenerate.sh' .agents | sort
```

Expected: the first prints nothing. The second prints exactly these six files, and nothing else:

```text
.agents/prompts/project.md
.agents/rules/conformance.md
.agents/rules/slop.md
.agents/skills/implementer/SKILL.md
.agents/skills/pipeline-law/SKILL.md
.agents/skills/spec-writer/SKILL.md
```

`project.md` is on the list because of task 8. `slop-police` says "the regeneration entry point" without the path, so it is not.

- [ ] **Step 9: Run the conformance check**

Run: `$PY tools/check-conformance.py`
Expected: exit 0.

- [ ] **Step 10: Commit**

```bash
git add .agents/rules/conformance.md .agents/rules/slop.md .agents/skills/slop-police/SKILL.md \
  .agents/skills/pipeline-law/SKILL.md .agents/skills/spec-writer/SKILL.md .agents/skills/implementer/SKILL.md
git commit -F - <<'EOF'
chore: name the regeneration entry point beside the check

conformance.md "Text only" now names tools/regenerate.sh as the second program.
The rules and roles that called the check the only program follow.
The pipeline's verification runs the regeneration too.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
EOF
```

---

### Task 10: Follow the howp skill rename and the README's new sections (consequences of §5.3 and §6.1)

**Files:**
- Modify: `.agents/rules/conformance.md:113-115`
- Modify: `.agents/rules/slop.md:78-80`
- Modify: `.agents/skills/issue-court/SKILL.md:7-8`
- Modify: `.agents/skills/tracker-clerk/SKILL.md:7-8`
- Modify: `.agents/skills/repo-police/SKILL.md:7-8`, `:312-315`

**Interfaces:**
- Consumes: plan 04 renames `plugins/howp/skills/howp/` to `plugins/howp/skills/forecast/` (conventions, Skills). Plan 06 restructures `README.md`: spec §6.1 drops the `Compatibility` section and puts the four client blocks under **Install**.
- Produces: nothing a later task reads.

Execute this task after plans 04 and 06 have landed on the branch. Before then each new path or heading names something not yet in the tree.

- [ ] **Step 1: Run the test and see it fail**

```bash
grep -rn 'skills/howp/references\|Compatibility section' .agents
test -d plugins/howp/skills/forecast && grep -n '^## Install' README.md
```

Expected: the first prints six lines. The second prints `## Install` once plans 04 and 06 have landed; if it prints nothing, stop and run this task later.

- [ ] **Step 2: The release-written path**

`conformance.md`, current text (lines 113–115):

````text
`version` in `plugins/howp/plugin.json`, the whole of
`plugins/howp/binaries.json` and
`plugins/howp/skills/howp/references/commands.md` are written by the release
````

New text:

````text
`version` in `plugins/howp/plugin.json`, the whole of
`plugins/howp/binaries.json` and
`plugins/howp/skills/forecast/references/commands.md` are written by the release
````

`slop.md`, current text (lines 78–80):

````text
- **What a release writes.** `version` in `plugins/howp/plugin.json`,
  `plugins/howp/binaries.json` and
  `plugins/howp/skills/howp/references/commands.md` are machine-written,
````

New text:

````text
- **What a release writes.** `version` in `plugins/howp/plugin.json`,
  `plugins/howp/binaries.json` and
  `plugins/howp/skills/forecast/references/commands.md` are machine-written,
````

- [ ] **Step 3: The README section the roles point at**

In each of `issue-court` (lines 7–8), `tracker-clerk` (lines 7–8) and `repo-police` (lines 7–8), current text:

````text
`README.md`'s Compatibility section lists.
````

The line is the start of a longer line in each file: `issue-court` continues " You run unattended once a day", `tracker-clerk` continues " You run unattended once a day,", `repo-police` continues " Your job is to keep it looking". Keep the rest of each line.

New text, replacing only the quoted prefix, in all three files:

````text
`README.md`'s Install section lists.
````

`repo-police`, current text (lines 312–315):

````text
4. Get a reader from the plugin to a **sourced** install path for every
   surface the root README's Compatibility section lists — either in the
   plugin README itself or by pointing at the root README's per-client
   section. A command stated without a source is the graver finding; a
````

New text:

````text
4. Get a reader from the plugin to a **sourced** install path for every
   surface the root README's Install section lists. The plugin README may
   give it, or point at the root README's Install section. A command
   stated without a source is the graver finding; a
````

- [ ] **Step 4: Run the test and see it pass**

```bash
grep -rn 'skills/howp/references\|Compatibility section' .agents
test -f plugins/howp/skills/forecast/references/commands.md && echo path-ok
grep -c "Install section lists" .agents/skills/issue-court/SKILL.md .agents/skills/tracker-clerk/SKILL.md .agents/skills/repo-police/SKILL.md
```

Expected: the first prints nothing. The second prints `path-ok`. The third prints `1`, `1` and `2`.

- [ ] **Step 5: Run the conformance check**

Run: `$PY tools/check-conformance.py`
Expected: exit 0.

- [ ] **Step 6: Commit**

```bash
git add .agents/rules/conformance.md .agents/rules/slop.md .agents/skills/issue-court/SKILL.md \
  .agents/skills/tracker-clerk/SKILL.md .agents/skills/repo-police/SKILL.md
git commit -F - <<'EOF'
chore: follow the forecast skill rename and the README's Install section

The release-written commands.md now sits under skills/forecast.
The roles point at the README section that lists the clients.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
EOF
```

---

### Task 11: The repository policy allows the files the other plans create (review addition B)

**Files:**
- Modify: `.agents/policies/repository.yaml:2-6`, `:19-27`

**Interfaces:**
- Consumes: the files plans 02, 03–07 and 08 create or edit.
- Produces: nothing a later task reads.

The file's shape stays: a `description`, then `paths.allow` as globs, then the comment explaining why `deny` is empty, then `redact` and `confirmations`. Only `allow` grows, and the `description` names what it now covers. The review listed fourteen paths. Existing globs already cover nine of them, so adding them again would be two entries for one path:

| Requested | Already covered by |
| :-- | :-- |
| `.github/ISSUE_TEMPLATE/*`, `.github/workflows/*.yml` | `".github/**"` |
| `tools/regenerate.sh`, `tools/templates/*`, `tools/package.json`, `tools/package-lock.json` | `"tools/**"` |
| `plugins/*/LICENSE`, `plugins/*/CHANGELOG.md`, `plugins/*/.claude-plugin/plugin.json` | `"plugins/**"` |

The five the list adds are `CONTRIBUTING.md`, `SUPPORT.md`, `docs/**`, `release-please-config.json` and `.release-please-manifest.json`. Two more files that other plans change are outside every glob, so this task adds them too. `SECURITY.md` is rewritten by plan 06 (spec §6.2). `.gitattributes` is created by plan 02 (conventions, `tools/regenerate.sh`).

- [ ] **Step 1: Run the test and see it fail**

```bash
grep -nE '"(CONTRIBUTING|SUPPORT|SECURITY)\.md"|"docs/\*\*"|release-please|"\.gitattributes"' .agents/policies/repository.yaml
```

Expected: nothing.

- [ ] **Step 2: The description**

Current text (lines 3–6):

````text
description: >-
  Ordinary maintenance: the plugin packages, the conformance check, the
  README, the workflow. Narrow, because the repository is small and public
  and its whole value is that its claims hold.
````

New text:

````text
description: >-
  Ordinary maintenance: the plugin packages, the conformance check and the
  regeneration script, the root documents, the workflows and the release
  configuration. Narrow, because the repository is small and public and its
  whole value is that its claims hold.
````

- [ ] **Step 3: The allow list**

Current text (lines 19–27):

````text
paths:
  allow:
    - ".agents/**"
    - ".claude/rules"
    - ".claude-plugin/**"
    - ".github/**"
    - "plugins/**"
    - "tools/**"
    - "README.md"
````

New text:

````text
paths:
  allow:
    - ".agents/**"
    - ".claude/rules"
    - ".claude-plugin/**"
    - ".github/**"
    - "plugins/**"
    - "tools/**"
    - "docs/**"
    - "README.md"
    - "CONTRIBUTING.md"
    - "SUPPORT.md"
    - "SECURITY.md"
    - ".gitattributes"
    - "release-please-config.json"
    - ".release-please-manifest.json"
````

- [ ] **Step 4: Run the test and see it pass**

```bash
grep -cE '"(CONTRIBUTING|SUPPORT|SECURITY)\.md"|"docs/\*\*"|release-please|"\.gitattributes"' .agents/policies/repository.yaml
$PY -c "
import yaml
d = yaml.safe_load(open('.agents/policies/repository.yaml'))
a = d['paths']['allow']
assert len(a) == len(set(a)) == 14, a
assert d['paths']['deny'] == [], d['paths']['deny']
print('policy OK')"
grep -c 'A deny entry here would make that' .agents/policies/repository.yaml
```

Expected: the first prints `7`. The second prints `policy OK`: fourteen entries, no duplicates, `deny` still empty. The third prints `1`: the comment giving the reason stays.

- [ ] **Step 5: Run the conformance check**

Run: `$PY tools/check-conformance.py`
Expected: exit 0.

- [ ] **Step 6: Commit**

```bash
git add .agents/policies/repository.yaml
git commit -F - <<'EOF'
chore: allow the files the rework adds to the repository policy

The policy now allows the community files, docs, SECURITY.md, .gitattributes and the release-please configuration.
Existing globs already cover the new tools, workflows and package files.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
EOF
```

---

### Task 12: What release-please writes is release-written too (review addition C)

**Files:**
- Modify: `.agents/rules/conformance.md:119-120`
- Modify: `.agents/rules/claims.md:48`
- Modify: `.agents/rules/slop.md:81-82`
- Modify: `.agents/skills/slop-police/SKILL.md:312`, `:365-366`
- Modify: `.agents/skills/pipeline-law/SKILL.md:674-680`
- Modify: `.agents/skills/implementer/SKILL.md:253`
- Modify: `AGENTS.md:59-60` (the "Versions" bullet; the coordinator put it in this cluster)

**Interfaces:**
- Consumes: spec §7.1. Plan 07 writes `.github/workflows/release-please.yml`, with `extra-files` for `plugin.json`'s `$.version` and `changelog-path: CHANGELOG.md`, for every package except howp.
- Produces: nothing a later task reads.

The howp sentences stay as they are. Each edit adds text after them, and none touches the lines task 10 edits. The pipeline's forbidden-path table and the Implementer's staged-path check follow in the same task. Otherwise `conformance.md` would forbid a hand edit to `CHANGELOG.md` or `.release-please-manifest.json` that the pipeline still allows. `AGENTS.md`'s "Versions" bullet follows too.

- [ ] **Step 1: Run the test and see it fail**

```bash
grep -rn 'release-please\|CHANGELOG' .agents/rules .agents/skills/pipeline-law/SKILL.md .agents/skills/implementer/SKILL.md
grep -n 'three release-written\|three files the release' .agents/skills/slop-police/SKILL.md
```

Expected: the first prints nothing. The second prints two lines.

- [ ] **Step 2: `conformance.md` "Versions"**

Current text (lines 118–120):

````text
artifact: a hand edit asserts a version, a digest or a target that no
release produced, and the next release overwrites it without noticing.
**The catalogue index carries no version at all**: `.claude-plugin/marketplace.json`
````

New text:

````text
artifact: a hand edit asserts a version, a digest or a target that no
release produced, and the next release overwrites it without noticing.
The other five packages are released by `.github/workflows/release-please.yml`.
It writes `version` in each of their `plugins/<name>/plugin.json` files, each
`plugins/<name>/CHANGELOG.md` and `.release-please-manifest.json`, and nothing
else writes them. Nobody edits those by hand either, for the same reason.
**The catalogue index carries no version at all**: `.claude-plugin/marketplace.json`
````

- [ ] **Step 3: `claims.md`, the rule on released artifacts**

Current text (line 48):

````text
  tell a fresh one from one the last release falsified.
````

New text:

````text
  tell a fresh one from one the last release falsified. For the other five
  packages, the release-please workflow rewrites `plugin.json`'s `version`,
  `CHANGELOG.md` and `.release-please-manifest.json`, and no other file.
````

- [ ] **Step 4: `slop.md` "What a release writes"**

Current text (lines 81–82):

````text
  and `conformance.md` says by whom. A wrong sentence in one is a defect of
  the release job, in the repository that runs it — never judged here.
````

New text:

````text
  and `conformance.md` says by whom. A wrong sentence in one is a defect of
  the release job, in the repository that runs it — never judged here.
  `version` in the other five `plugins/<name>/plugin.json` files, each
  `plugins/<name>/CHANGELOG.md` and `.release-please-manifest.json` are
  machine-written too, by the release-please workflow, and are never judged
  here either.
````

- [ ] **Step 5: `slop-police` stops counting three**

Current text (line 312):

````text
- the three release-written files
````

New text:

````text
- the release-written files
````

Current text (lines 365–366):

````text
`tools/schemas/**` — a verbatim copy of somebody else's text. Not the
three files the release writes.
````

New text:

````text
`tools/schemas/**` — a verbatim copy of somebody else's text. Not the
files a release writes.
````

- [ ] **Step 6: `pipeline-law` forbids `CHANGELOG.md`**

Current text (lines 674–680):

````text
| `plugins/*/skills/*/references/commands.md` | the same |
| `tools/schemas/**` | a verbatim copy of a published schema |
| `.agents/**` | the rules the machine is governed by |

`.agents/rules/conformance.md` carries the rule on the first three. A finding
that one of those files is wrong is a defect of the release job in the
repository that runs it. The honest outcome here is a comment saying so.
````

New text:

````text
| `plugins/*/skills/*/references/commands.md` | the same |
| `plugins/*/CHANGELOG.md` | written by the release-please workflow, never by hand |
| `.release-please-manifest.json` | the same |
| `tools/schemas/**` | a verbatim copy of a published schema |
| `.agents/**` | the rules the machine is governed by |

`.agents/rules/conformance.md` carries the rule on the first five. A finding
that one of those files is wrong is a defect of the release job in the
repository that runs it. The honest outcome here is a comment saying so.
````

- [ ] **Step 7: `implementer` checks staged release-please files**

Current text (line 253):

````text
      | grep -E '^(tools/schemas/|\.agents/|plugins/[^/]+/binaries\.json$|plugins/[^/]+/skills/[^/]+/references/commands\.md$)' \
````

New text:

````text
      | grep -E '^(tools/schemas/|\.agents/|plugins/[^/]+/binaries\.json$|plugins/[^/]+/CHANGELOG\.md$|\.release-please-manifest\.json$|plugins/[^/]+/skills/[^/]+/references/commands\.md$)' \
````

- [ ] **Step 8: `AGENTS.md` — the "Versions" bullet names both release jobs**

Current text (lines 59–60):

````text
- **Versions**, on the three files a release job in another repository writes
  and nobody edits by hand.
````

New text:

````text
- **Versions**, on the files a release writes and nobody edits by hand. For
  howp, the how-possible release job writes three. For the other five
  packages, release-please writes `version`, `CHANGELOG.md` and
  `.release-please-manifest.json`.
````

- [ ] **Step 9: Run the test and see it pass**

```bash
grep -c 'release-please' .agents/rules/conformance.md .agents/rules/claims.md .agents/rules/slop.md .agents/skills/pipeline-law/SKILL.md
grep -c 'release-please-manifest' .agents/rules/conformance.md .agents/rules/claims.md .agents/rules/slop.md .agents/skills/pipeline-law/SKILL.md
grep -n 'how-possible release job\|three files a release job' AGENTS.md
test -L CLAUDE.md && echo symlink-kept
grep -n 'three release-written\|three files the release' .agents/skills/slop-police/SKILL.md
grep -c 'edits any of the three by hand, ever' .agents/rules/conformance.md
printf 'plugins/triz/CHANGELOG.md\n.release-please-manifest.json\nplugins/triz/README.md\n' \
  | grep -E '^(tools/schemas/|\.agents/|plugins/[^/]+/binaries\.json$|plugins/[^/]+/CHANGELOG\.md$|\.release-please-manifest\.json$|plugins/[^/]+/skills/[^/]+/references/commands\.md$)'
grep -c 'plugins/howp/binaries.json' .agents/rules/conformance.md .agents/rules/slop.md
```

Expected: the first prints `2` for each file. The second prints `1` for each file. The third prints one line, the new bullet. The fourth prints `symlink-kept`. The fifth prints nothing. The sixth prints `1`: the howp sentence "Nobody edits any of the three by hand, ever." is still there, unchanged. The seventh prints `plugins/triz/CHANGELOG.md` and `.release-please-manifest.json`, and not the README: the Implementer's pattern catches both release-please files. The eighth prints `1` for each file: the howp sentences stay.

- [ ] **Step 10: Run the conformance check**

Run: `$PY tools/check-conformance.py`
Expected: exit 0.

- [ ] **Step 11: Commit**

```bash
git add .agents/rules/conformance.md .agents/rules/claims.md .agents/rules/slop.md \
  .agents/skills/slop-police/SKILL.md .agents/skills/pipeline-law/SKILL.md .agents/skills/implementer/SKILL.md AGENTS.md
git commit -F - <<'EOF'
chore: treat what release-please writes as release-written

release-please writes version, CHANGELOG.md and its manifest for five packages, so nobody edits them by hand.
The rules, AGENTS.md, the pipeline's forbidden paths and the Implementer's staged-path check now say so.
The howp sentences are unchanged.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
EOF
```

---

### Task 13: Verify the cluster as a whole (spec §10, the `.agents/` line)

**Files:** none changed.

**Interfaces:**
- Consumes: tasks 1–12.
- Produces: the evidence for spec §10: "`.agents/` has no clause demanding the diary; `agent-police` carries the external list; every issue-listing clause names the `police-report` population."

- [ ] **Step 1: No clause demands the diary**

```bash
grep -rnE 'was not verified|"not verified"|unverified (stated|means)|claims discipline.s sentences|install section' .agents
```

Expected: nothing.

- [ ] **Step 2: The old markers live only in the definition**

```bash
grep -rlE 'repo-audit-routine|slop-police-fingerprint|agent-police-fingerprint' .agents .claude/agents
```

Expected: exactly `.agents/skills/github-needs/SKILL.md`.

- [ ] **Step 3: Every role that lists issues names the population**

```bash
for f in issue-court tracker-clerk pipeline-clerk pipeline-law repo-police slop-police agent-police; do
  printf '%s ' "$f"; grep -c 'machine population' ".agents/skills/$f/SKILL.md"
done
grep -rnE 'whole open list|without `police-report`|List every open issue with|List open issues carrying' .agents
```

Expected: every count is at least `1`. The grep prints nothing.

- [ ] **Step 4: `agent-police` carries the external list**

```bash
grep -n '^## External sources$\|external-disagreement' .agents/skills/agent-police/SKILL.md
grep -c 'agent-plugins.org/specification\|agentskills.io/specification\|code.claude.com/docs/en/plugins\|learn.chatgpt.com/docs/plugins\|hermes-agent.nousresearch.com\|can1357/oh-my-pi\|standard-readme\|keepachangelog.com\|semver.org\|awesome-claude-code' .agents/skills/agent-police/SKILL.md
```

Expected: the first prints the heading and one `external-disagreement` line. The second prints at least `10`.

- [ ] **Step 5: The protected line stays**

```bash
grep -n '^- \*\*The instructions.\*\* `.agents/\*\*`. Read, never judged.$' .agents/rules/slop.md
```

Expected: one line.

- [ ] **Step 6: The review additions stand**

```bash
grep -rn 'publishes no plugins documentation' .agents
grep -c 'developers.openai.com/plugins/build/plugins.md' .agents/rules/conformance.md
grep -n 'Five roles read `github-needs`' AGENTS.md
grep -c 'release-please' .agents/rules/conformance.md .agents/rules/claims.md .agents/rules/slop.md
```

Expected: the first prints nothing. The second prints `1`. The third prints one line. The fourth prints `1` for each file.

- [ ] **Step 7: Every front matter, the manifest and the policy parse**

```bash
$PY - <<'EOF'
import glob, os, yaml
yaml.safe_load(open('.agents/manifest.yaml'))
yaml.safe_load(open('.agents/policies/repository.yaml'))
files = sorted(glob.glob('.agents/skills/*/SKILL.md') + glob.glob('.claude/agents/*.md'))
for f in files:
    d = yaml.safe_load(open(f).read().split('---')[1])
    want = os.path.basename(os.path.dirname(f)) if f.endswith('SKILL.md') else os.path.basename(f)[:-3]
    assert d['name'] == want, f
print(len(files), 'front matters OK')
EOF
```

Expected: `20 front matters OK`.

- [ ] **Step 8: The conformance check and a clean tree**

```bash
$PY tools/check-conformance.py; echo "exit=$?"
git status --porcelain
```

Expected: `conformance OK: …`, `exit=0`, and no output from `git status --porcelain`.

- [ ] **Step 9: No commit**

This task changes nothing. If any step failed, reopen the task that owns the passage, fix it there, and commit under that task's message.

---

## Decisions taken at review

The plan's first draft left nine questions open. The coordinator's review settled them:

1. **Confirmed.** The Pipeline Clerk is a fourth filer, and its parts carry `police-fingerprint: pipeline-clerk …` (task 7).
2. **Accepted as written.** A closed issue with an old marker leaves the population. A finding declined under an old marker can be filed once more under the new line.
3. **Confirmed.** A filer that cannot confirm `police-report` files nothing (task 2). The self-repair of a missing label goes.
4. **Confirmed.** The Pipeline Clerk reads `pipeline/intake` only inside the population (task 7).
5. **Accepted.** The Agent Police's fingerprint value is `<path>::<concept>::<pass>` (task 4).
6. **Accepted.** The skill and binding descriptions carry the §3.2 update (task 3). `AGENTS.md`'s count of `github-needs` readers follows the manifest (task 2, step 7, addition D).
7. **Corrected.** The Codex sentence in `conformance.md` now cites Codex's plugin documentation (task 8, step 5, addition A).
8. **Added.** The policy allows the new files (task 11, addition B). The release-written files include what release-please writes (task 12, addition C).
9. **Noted.** The how-possible release job writes `references/commands.md` at the old howp path. Plans 04 and 07 own that.

## Later review decisions

- The "Versions" bullet of `AGENTS.md` names what the how-possible release job writes and what release-please writes (task 12, step 8).
- `.release-please-manifest.json` is release-written: task 12 lists it beside `version` and `CHANGELOG.md` in every sentence, in the pipeline's forbidden paths and in the Implementer's check.
