---
name: hp
description: >
  Личный вероятностный дашборд: превращает интересы пользователя в измеримые
  вопросы, находит под них рынки предсказаний (Polymarket, Manifold) и ведёт
  markdown-дашборд вероятностей с историей, движениями и сводками. Применять,
  когда пользователь просит настроить, обновить или объяснить свой
  вероятностный дашборд hp, пройти опросник интересов или установить сборщик
  данных. Детерминированные шаги выполняются Rust-бинарями, скачиваемыми из
  публичных релизов под платформу пользователя; ключи внешних LLM-сервисов не
  требуются.
license: MIT
---

# hp — личный вероятностный дашборд

**Статус: заготовка.** Плагин опубликован для обкатки канала установки;
рабочая версия скилла появится вместе с первым релизом бинарей. Ничего не
скачивает и не настраивает — только объясняет замысел.

## Что это будет

- Вместо новостной ленты — вероятности: интересы разворачиваются в измеримые
  вопросы, вопросы закрываются рынками предсказаний, дашборд показывает
  «что стало вероятнее и насколько», с честными оговорками.
- Детерминированная работа (сбор котировок, подсчёты, детектор резких
  движений, рендер, хранение) — набор Rust-бинарей. Скилл скачивает их из
  публичных релизов под вашу систему и проверяет контрольные суммы.
- Агентные роли (опросник интересов, поиск рынков, верификация критериев,
  пояснения) выполняет ваш агент по инструкциям скилла — внешние API-ключи
  не нужны.
- Настройка по средам: macOS (сборщик по расписанию через launchd — только
  после вашего явного согласия, объяснённого простым языком), GitHub-форк
  (workflow: бесплатный сбор по крону, агентное — вручную), локальный режим
  (данные в папке скилла, в git не попадают).

## Пока скилл в разработке

Следить за проектом и релизами: https://github.com/Akurganow/ai-plugins
